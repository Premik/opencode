# Overview

opencode is a monorepo (managed with Bun workspaces + Turborepo) for an AI coding assistant and its surrounding platform. The project bundles a CLI, a terminal UI (TUI), a web app, a desktop app, shared core logic, an LLM abstraction layer, protocol/schema packages, and several SDKs.

Key architecture points:
- Multiple interfaces: a CLI/TUI (SolidJS + @opentui), a shared web app (`packages/app`, SolidJS), a desktop Electron wrapper (`packages/desktop`), and a plugin system (`@opencode-ai/plugin`).
- Core business logic and API server live in `packages/opencode`. The session runtime preserves durable conversational history while dynamically assembling system context (instructions, tool descriptions, user-defined context sources) for each provider turn.
- V2 separates concerns across packages: Schema (lightweight shared types), Protocol (API endpoint construction), Server (concrete HTTP handlers), Client (generated Promise/Effect APIs), and SDK (composed in-process host). The `HttpApi` is authoritative for public client capabilities.
- System Context is built from independently observed, stable-keyed Context Sources; changes sampled lazily at Safe Provider-Turn Boundaries (never pushed asynchronously). A Context Epoch spans one immutable baseline; compaction starts a new epoch.
- Client tiers: a zero-Effect Promise client (`@opencode-ai/client`) and an Effect-native client (`@opencode-ai/client/effect`). The SDK (`@opencode-ai/sdk-next`) composes client, core, and server in-process without network I/O.

# Source tree

## Root (REFINED)

- `AGENTS.md` — Comprehensive contributor guide covering branch naming (max 3 hyphenated words), conventional commit format (`type(scope): summary`), and detailed TypeScript style rules (inline logic, avoiding `try/catch`, Bun APIs, functional array methods, Effect generator patterns, Drizzle schema snake_case, testing without mocks). Documents the V2 Session Core architecture rules for durable prompt admission, session execution ownership, location-scoped runners, delivery vocabulary (steer/queue), and system context organization.
- `CONTEXT.md` — Detailed domain glossary and architecture reference for the Session Runtime. Formally defines ~40 terms (System Context, Context Source, Context Epoch, Session Drain, Model Tool Output, etc.) with precise interrelationship rules, and documents the client-contract architecture (HTTP client generation, embedded vs. networked, SDK contract IR, emitter policies). Authoritative source for the V2 session model's vocabulary and invariants.
- `CONTRIBUTING.md` — Contributor guide covering dev setup, build commands, debugging techniques, and PR/issue policies. Explains monorepo layout, how `bun dev` maps to the built CLI, building a standalone executable, running against a different target directory, debugging with Bun `--inspect` (incl. VSCode config), and the trust/vouch system, issue templates, and PR expectations (issue-first, conventional commits, UI screenshots for UI changes).
- `README.md` — Top-level project README for OpenCode AI coding agent. Lists installation methods (npm, Homebrew, Scoop, pacman, Nix), desktop app beta downloads, custom install directory priority order, two built-in agents (build and plan) switchable via Tab, and links to docs, contributing, and community.
- `package.json` — Root Bun workspace config (1.3.14) spanning `packages/*`, `packages/console/*`, `packages/stats/*`, `packages/sdk/js`, `packages/slack`. Dev scripts: `dev` (CLI app), `dev:desktop`, `dev:web`, `dev:console`, `dev:storybook`, lint via oxlint, typecheck via Turbo, `postinstall` runs `fix-node-pty`. Notable deps: Effect (beta), Hono, Drizzle ORM, SolidJS, SST, Shiki, @opentui, with patched packages for AI SDKs, SolidJS, Effect, MCP.
- `sst.config.ts` — SST (Ion) infra config deploying OpenCode's cloud backend on Cloudflare (AWS secondary, us-east-1, stage-aware S3 protect/removal). Configures Stripe, Random, PlanetScale, Honeycomb providers. `run()` dynamically imports infra modules (`infra/stage`, `app`, `lake`, `stats`, `console`, `enterprise`, `monitoring`) conditional on stage and AWS features.
- `install` — Shell script for downloading/installing the OpenCode binary across Linux/macOS/Windows. Supports custom versions, local paths, `--no-modify-path`. Detects OS, arch (Rosetta-aware arm64 fallback), CPU feature level (AVX2 baseline for x64), musl libc (Alpine), platform archives. Downloads from GitHub releases with a curl-based progress bar, installs to `$HOME/.opencode/bin`, optionally modifies PATH in shell configs.
- `turbo.json` — Turbo repo task config defining `typecheck`, `build` (outputs to `dist/**`), and per-package `test` tasks for `opencode`, `@opencode-ai/core`, `@opencode-ai/app`, `@opencode-ai/ui`, `@opencode-ai/session-ui`. Global env passthrough: `CI`, `OPENCODE_DISABLE_SHARE`.

## Root folders

- `packages/` (32 packages, ~180KB total files) — REFINED — Monorepo packages: core (V2 session runtime, system context, tools, plugin system), opencode (V1 host: session orchestration, HTTP server, tools), tui (terminal UI on SolidJS), app (web app shell), ui (shared SolidJS components + theming), web (Astro docs + share viewer), cli (daemon + V1 CLI commands), client (generated Promise/Effect API clients), server (V2 HTTP handlers), protocol (HttpApi endpoint definitions), schema (shared Drizzle schemas + event types), sdk (hand-written JS SDK wrappers + OpenAPI codegen), llm (pluggable LLM runtime), console (cloud backend: auth, billing, workspace, Zen proxy), desktop (Electron wrapper).
- `.opencode/` (38 files) — REFINED — Repo's own opencode configuration: agents, commands, a translation glossary, an Effect-TS skill, TUI themes, and legacy plugin/tool experiments.
  - `opencode.jsonc` — Repo opencode config: schemas, empty provider/permission/MCP blocks, commented-out Effect references and legacy tools.
  - `tui.json` — Minimal TUI config registering a single local plugin (`tui-smoke.tsx`) with keybinds for modal/screen/dialog toggles (currently disabled).
  - `env.d.ts` (dropped — trivial)
  - `agent/` (2 files) — Hidden primary agent definitions: `duplicate-pr.md` (uses `opencode/claude-haiku-4-5` + `github-pr-search` tool to detect duplicate/related open PRs by keyword search); `triage.md` (uses `opencode/gpt-5.4-mini` + `github-triage` tool to assign issues to teams: TUI/Desktop/Web/Core/Inference/Windows).
  - `command/` (8 files) — Slash-command definitions (markdown) for repo automation: `ai-deps.md` (audits AI SDK deps for upgrade opportunities), `changelog.md` (generates `UPCOMING_CHANGELOG.md` from structured commit input), `commit.md` (subtask: `git diff`/`status` → commit+push w/ conventional-commit prefix), `issues.md` (`gh` CLI search of anomalyco/opencode issues), `learn.md` (analyzes session for discoveries → writes `AGENTS.md`), `rmslop.md` (diffs vs `dev`, strips AI slop), `spellcheck.md` (checks unstaged markdown), `translate.md` (`opencode/claude-opus-4-8` translates changed English copy into all languages in parallel).
  - `glossary/` (17 files) — Locale-specific translation glossaries supplementing the translator agent. Shared template with sections for Sources, Do Not Translate, Preferred Terms, Guidance, Avoid. README defines naming conventions (lowercase locale slugs) and contribution rules; the 16 language files capture community-backed phrasing/tone preferences and locale-specific do-not-translate terms.
  - `skills/effect/SKILL.md` — Effect-TS skill establishing Effect v4 / effect-smol as source of truth: directs the agent to clone the reference repo and search it before answering, with guidelines for `Effect.gen`/`Effect.fn`, Schema, layers, and testing conventions (`testEffect`, `it.live`).
  - `themes/mytheme.json` — Nord-inspired TUI theme: 16 nord color variables mapped to ~50 semantic tokens covering UI chrome, diff highlighting, markdown rendering, and syntax highlighting, each with dark/light variants.
  - `tool_old/` (2 files) — Legacy plugin tools: `github-pr-search.ts` (search anomalyco/opencode PRs via Search Issues API) and `github-triage.ts` (assign a GitHub issue to a random team member via Issues Assignees API).
  - `plugins_old/` (2 files) — Legacy/experimental TUI plugin assets: `tui-smoke.tsx` (31.3KB comprehensive smoke-test exercising the full @opentui plugin API — routes, dialogs, overlays, keymaps, post-process effects, theme install) and `smoke-theme.json` (companion Nord-based theme).
- `script/` (18 files) — REFINED — Repo automation: release/publish orchestration, changelog generation, translation pipeline, beta-branch management, stats collection, and dependency upgrades.
  - `beta.ts` — Beta branch automation: fetches all open PRs with a `beta` label, merges each sequentially using OpenCode to auto-resolve conflicts, runs a final typecheck+build smoke check, then force-pushes the validated beta branch.
  - `changelog.ts` / `raw-changelog.ts` — Changelog generation: `changelog.ts` delegates to `opencode run changelog` (supports `--from`/`--to`/`--variant`/`--quiet`/`--print`); `raw-changelog.ts` is the standalone generator querying GitHub API + local git history, categorizing commits into sections (Core, TUI, Desktop, SDK, Extensions) with Improvements/Bugfixes sub-types, tracking community contributors and revert pairing.
  - `duplicate-pr.ts` — Thin wrapper creating an OpenCode session with the `duplicate-pr` agent, sending a message + optional file attachment and printing the session's text response.
  - `publish.ts` / `version.ts` — Release orchestration: `publish.ts` bumps all `package.json` files, rebuilds JS SDK, publishes each sub-package (also tags/pushes/publishes GitHub release for non-preview); `version.ts` creates a draft GitHub release for the current version, generates changelog + release notes, outputs Actions step variables.
  - `sign-windows.ps1` — PowerShell for Azure Trusted Signing of Windows binaries in CI; installs the `TrustedSigning` module, resolves file paths, invokes `Invoke-TrustedSigning`, guards against running outside GitHub Actions.
  - `stats.ts` — Downloads tracker fetching GitHub release asset download counts and npm all-time stats for `opencode-ai`; appends a row to `STATS.md` with deltas and fires PostHog analytics events.
  - `translate-app.ts` / `.test.ts` / `.md` — Translation pipeline synchronizing product app translations across 17 locales: `translate-app.ts` inspects dictionaries for missing/extra keys and placeholder mismatches, spawns isolated OpenCode sessions with scoped agent configs, includes drift checking, model variant resolution, worktree snapshot comparison, bounded-concurrency parallel execution. `.test.ts` covers locale parsing, glossary mapping, drift detection, concurrency. `.md` is the agent prompt template.
  - `upgrade-opentui.ts` — Dependency upgrade utility for `@opentui/*` packages: scans all `package.json` files, updates catalog entries/overrides/dependency fields to a target version, runs `bun install`, validates the lockfile for stale entries; supports release and snapshot versions.
  - Trivial wrappers (dropped from detail): `format.ts` (prettier write), `generate.ts` (build SDK + OpenAPI JSON), `hooks` (installs pre-push git hook: `bun install` + typecheck), `release` (triggers `publish.yml` Actions workflow).
  - `github/` (2 files) — Automated GitHub issue/PR cleanup: `close-issues.ts` closes issues inactive 60+ days with "not planned" reason + comment, skipping team/bot issues; `close-prs.ts` closes stale PRs below a reaction-score threshold via GraphQL with retry/backoff, dry-run mode, cleanup label.
- `specs/` (14 files) — REFINED — Design spec documents (markdown) for V2 architecture, storage migration, and API design.
  - `project.md` — Defines the REST API surface for multi-project/multi-worktree sessions, listing endpoints for project/session CRUD, messages, revert, permissions, files, and provider/config/agent lookups. Notable: several query endpoints are flagged as "awkward" pending redesign.
  - `tui-package.md` — Complete 10-section migration plan to extract the TUI from `packages/opencode` into a standalone `@opencode-ai/tui` package, covering dependency rules (SDK-only boundary, no backend imports), ownership boundaries, and incremental migration. All sections marked completed.
  - `storage/` (2 files) — `effect-sqlite-package.md` (vendoring a Drizzle Effect SQLite adapter as generic `@opencode-ai/effect-drizzle-sqlite`, mirroring upstream API, 8-step migration); `remove-opencode-db.md` (full teardown of `packages/opencode/src/storage/db.ts` across 22 files in 5 migration groups — Runtime/Startup, Sync Transaction Boundary, Domain Repositories, Session/Message Reads, Legacy CLI/Admin; preserves nested transaction visibility and post-commit effect ordering).
  - `v2/` (10 files) — Core V2 design specs. Key docs: `config.md` (field-by-field V1→V2 config review), `session.md` (durable prompt admission, Context Epochs, steer/queue delivery, execution routing), `schema-changelog.md` (chronological contract changes), `provider-model.md` + `provider-policy.md` (catalog/policy system), `tools.md` (tool definition + execution pipeline), `catalog-config-plugin-lifecycle.md` (catalog transform design), `instructions.md` (porting guidelines), `todo.md` (remaining work tracker).
- `infra/` (8 files) — REFINED — SST (Ion) infrastructure modules deploying OpenCode's cloud backend on Cloudflare (AWS secondary). Major modules: `console.ts` (9.4KB — PlanetScale DB, auth/log Workers, SolidStart console app, Stripe Go/Black catalog + webhooks); `lake.ts` (10.3KB — S3 Tables data lake, Glue catalog, Athena workgroup 2TB cutoff, Kinesis Firehose Iceberg ingestion, ECS Fargate ingest); `monitoring.ts` (8.0KB — Honeycomb alerts via Discord webhook: model error rates, low TPS, provider errors, anomalous free-tier volume); `stats.ts` (7.8KB — PlanetScale stats DB, Iceberg `inference.event` table 50+ columns, Stats SolidStart app, `StatsSyncService` periodic Athena sync); `app.ts` (2.1KB — core Cloudflare Workers + static sites: API worker w/ Durable Object sync, Astro docs at `docs.*`, web app at `app.*`, secrets, R2 bucket). Minor: `enterprise.ts` (enterprise/teams SolidStart app), `secret.ts` (SST secrets wrapper + Honeycomb webhook password), `stage.ts` (derives deployment domain `opencode.ai`/`dev.opencode.ai`/`{stage}.dev.opencode.ai` + short domain `opncd.ai`).
- `nix/` (6 files) — REFINED — Nix packaging derivations for CLI and desktop app. `opencode.nix` (2.8KB — CLI single-binary w/ embedded schema, ripgrep on PATH, shell completions); `desktop.nix` (2.8KB — Electron wrapper, platform-specific install); `node_modules.nix` (2.0KB — fixed-output `bun install` w/ platform-pinned CPU/OS, canonicalizes symlinks) + `scripts/` (`canonicalize-node-modules.ts` dedup `.bun` cache → flat tree by highest semver; `normalize-bun-binaries.ts` rebuilds `.bin` symlinks); `hashes.json` (SRI hashes across 4 platforms).
- `sdks/` (13 files) — REFINED — Third-party SDKs/extensions. `vscode/` — VS Code extension (`sst-dev` publisher): `src/extension.ts` (3.9KB — three commands: reuse/open split terminal running `opencode --port <random>`, insert `@file#L123` reference w/ selection range via local HTTP endpoint); `package.json` (2.9KB — manifest w/ keybindings `cmd+esc`/`cmd+shift+esc`/`cmd+alt+k`, esbuild bundling); `esbuild.js` (1.2KB — bundles w/ `vscode` externalized, `--production`/`--watch`); `README.md`; `script/release` + `script/publish` (tag bump + `.vsix` publish to VS Code marketplace + Open VSX).
- `github/` (10 files) — REFINED — GitHub Action for running OpenCode in CI on issues/PRs. `index.ts` (30.2KB — core entrypoint: spawns local opencode server, fetches triggering issue/PR comment + full GitHub GraphQL context, routes through three workflows — issue comment → create branch + open PR; local PR comment → checkout/run/push; fork PR → set up fork remote + push; handles image attachments, session events via SSE, git credential management, GitHub App token exchange via OIDC); `action.yml` (2.4KB — composite Action: caches opencode binary, installs via curl if not cached, runs `opencode github run` w/ env vars for model/agent/share/prompt/mentions/variant/OIDC); `README.md` (6.0KB — usage examples, installation, config reference, dev guide w/ mock payloads); `script/release` + `script/publish` (tag bump + `latest` tag management).

## packages/

- `core/` (3976KB, 488 files) — REFINED — See `packages/core/src/` below.
- `opencode/` (17832KB, 758 files) — REFINED — See `packages/opencode/src/` below.
- `tui/` (1924KB, 241 files) — REFINED — See `packages/tui/src/` below.
- `app/` — See `packages/app/src/` below.
- `ui/` — See `packages/ui/src/` below.
- `web/` — See `packages/web/src/` below.
- `cli/` (156KB, 25 files) — REFINED — See `packages/cli/src/` below.
- `client/` (292KB, 21 files) — REFINED — See `packages/client/src/` below.
- `server/` (160KB, 31 files) — REFINED — See `packages/server/src/` below.
- `protocol/` (148KB, 26 files) — REFINED — See `packages/protocol/src/` below.
- `schema/` (356KB, 74 files) — REFINED — See `packages/schema/src/` below.
- `sdk/` — See `packages/sdk/` below.
- `llm/` (1720KB, 152 files) — REFINED — See `packages/llm/src/` below.
- `console/` — See `packages/console/` below.
- `desktop/` (9184KB, 250 files) — REFINED — See `packages/desktop/` below.

## packages/core/src/ (REFINED)

V2 core: durable session execution loop, system context algebra, built-in tools, plugin system with 31 provider facades, database/event infrastructure, and location-scoped service composition. All services are Effect-based with either `makeLocationNode` (location-scoped) or `makeGlobalNode` (process-global) lifecycle from `effect/`. SQL files define Drizzle tables with snake_case fields; several top-level files are pure re-export barrels for schema types.

**Session runtime** (`session/`, 25 files, 208KB):
- `runner/` (6 files, 52KB) — Serialized V2 execution loop. `llm.ts` (20.7KB — core run loop: loads session -> selects agent -> initializes system context -> resolves model -> streams exactly one `llm.stream()` per turn, handles overflow compaction recovery, steer/queue input promotion at safe boundaries). `publish-llm-event.ts` (incremental event persistence for one provider turn, tracks tool lifecycle in `Map`, fragment streaming). `to-llm-message.ts` (projects V2 history -> canonical LLM `Message[]`). `model.ts` (catalog -> concrete model resolution, tagged errors). `index.ts` (barrel + `Interface`/`RunError`). `max-steps.ts` (step-limit prompt constant).
- `compaction.ts` — Auto-summarizes older messages when token budget exceeded (reverse-scan split point -> structured summary prompt -> LLM call). Defaults: 20K buffer, 8K keep.
- `context-epoch.ts` — Persisted System Context snapshot per session; `prepare` loads + reconciles epoch (Unchanged/ReplacementReady/Update), `reset` on move/revert.
- `input.ts` — Durable prompt admission/promotion lifecycle (`admit`/`promoteSteers`/`promoteNextQueued`); exact-retry reconciliation via `equivalent`/`matchesPrompt`. `LifecycleConflict` tagged error.
- `message-updater.ts` — Event-driven state machine building session messages from `SessionEvent` variants (immer-based immutable updates, in-memory + DB-backed adapters).
- `projector.ts` — Event projector: session events -> persistent DB state (handles V1+V2 events, revert deletion, move location update). Global node.
- `revert.ts` — Three-phase revert (stage/clear/commit) with file snapshot restore.
- `run-coordinator.ts` — Per-key execution serializer (joins same-session resumes, coalesces wakeups, concurrent across sessions, `FiberSet.makeRuntime`).
- `sql.ts` — Drizzle tables: Session/SessionMessage/SessionInput/SessionContextEpoch/Todo + V1 Message/Part (cascade-delete on session FK).
- Supporting files: `store.ts` (data access), `history.ts` (message loading/decoding), `info.ts` (row->typed struct), `todo.ts` (per-session todo items), `execution.ts`+`execution/local.ts` (process-global execution controller, `noopLayer` for headless), `schema.ts`/`prompt.ts`/`message.ts`/`event.ts` (schema re-exports), `error.ts` (tagged errors).

**Config** (`config/`, 20 files, 96KB): Top-level files are Effect `Schema` definitions per domain -- `agent.ts`, `provider.ts` (model+cost+limit+variants), `mcp.ts` (local/remote+OAuth+timeouts), `lsp.ts`, `markdown.ts` (frontmatter parser+sanitizer via `gray-matter`), `compaction.ts`/`formatter.ts`/`attachments.ts`/`reference.ts`/`command.ts`/`tool-output.ts`/`watcher.ts`, `experimental.ts` (policies), `plugin.ts`. `plugin/` subdir (6 files) -- runtime loaders using `define()`: `agent.ts` (discovers agents from MD+JSON, merges permissions, V1 migration), `command.ts`, `external.ts` (npm/local plugins via `Npm.Service`), `provider.ts` (providers+models -> catalog + integrations), `reference.ts` (git/local sources), `skill.ts` (dir/URL sources).

**Built-in tools** (`tool/`, 20 files, 156KB) -- All follow: resolve path -> check `external_directory` permission -> assert action -> perform work -> return typed output. Each is a self-contained `makeLocationNode`.
- Foundation: `tool.ts` (`Tool.make()` constructs opaque `Definition`, WeakMap-cached `Runtime` with `settle()`), `registry.ts` (merges application + location-local tools, materializes with permissions), `tools.ts`/`application-tools.ts` (registration interfaces), `builtins.ts` (composition root, 12 tools).
- File tools: `edit.ts` (exact text replacement, stale-content guard via `writeIfUnchanged`), `write.ts` (create/overwrite, BOM-aware), `read.ts`+`read-filesystem.ts` (file/dir reading, 64KB magic-byte detection, image base64 <20MB, paginated text with 2000-char line truncation), `apply-patch.ts` (unified patch application, partial progress on failure).
- Search: `glob.ts` (ripgrep glob), `grep.ts` (ripgrep regex, grouped output format).
- Shell: `bash.ts` (command execution, workdir resolution via `LocationMutation`, 1MB capture, 10min max timeout).
- Web: `webfetch.ts` (HTTP fetch, Cloudflare challenge retry, HTML->Markdown via Turndown), `websearch.ts` (Exa/Parallel providers, JSON-RPC MCP, deterministic hash-based split).
- Other: `question.ts` (user interaction via `QuestionV2.Service`), `skill.ts` (skill loading, <=10 co-located files), `todowrite.ts` (session todo-list), `http-body.ts` (shared bounded body collection).

**Plugin system** (`plugin/`, 45 files, 264KB):
- Core: `internal.ts` (bootstrapper, `define()` identity wrapper, registers all built-in plugins via `forkScoped` in `State.batch`), `host.ts` (`PluginHost` -> `PluginContext` with per-domain `.transform()`/`.reload()` + dynamic child plugins), `promise.ts` (Promise->Effect adapter bridge).
- Built-in plugins: `agent.ts` (6 agents: build, plan, general, explore, hidden compaction+title), `command.ts` (init+review), `skill.ts` (customize-opencode), `models-dev.ts` (syncs model/provider metadata from models.dev -> catalog, auto-reload on refresh), `variant.ts` (auto-generates high/max GLM-5 variants).
- `provider/` (35 files, ~80KB) -- 31 provider facades mapping provider-specific APIs -> common LLM protocol, each `define()`-wrapped. **Large facades** (>1.5KB): `opencode.ts` (12.8KB -- console/proxy, device OAuth, dynamic catalog injection, public mode fallback), `openai.ts` (10.7KB -- full OAuth for ChatGPT Pro/Plus, PKCE+device flow), `google-vertex.ts` (3 flavors: native/OpenAI-compatible/Anthropic), `amazon-bedrock.ts` (standard+Mantle, cross-region prefixes), `snowflake-cortex.ts`/`azure.ts`/`cloudflare-ai-gateway.ts`/`cloudflare-workers-ai.ts`/`gitlab.ts`/`github-copilot.ts`/`sap-ai-core.ts`/`openrouter.ts`/`llmgateway.ts`. **Small facades** (<=1.5KB, 21 files) -- thin config wrappers in 3 patterns: 10 SDK-only (`alibaba`/`cohere`/`deepinfra`/`gateway`/`google`/`groq`/`mistral`/`perplexity`/`togetherai`/`venice`/... -- each ~15 lines, hooks `ctx.aisdk.sdk()`), 4 SDK+catalog headers (`anthropic`/`cerebras`/`vercel`/`xai`), 3 catalog-only (`kilo`/`nvidia`/`zenmux`), `openai-compatible.ts` (generic fallback), `dynamic.ts` (escape hatch via `npm add`).
- (dropped: `layer-map.example.ts` -- tutorial file, not runtime)

**System context** (`system-context/`, 3 files, 15KB): `index.ts` -- typed stable-keyed (`domain/name`) `Source<A>` algebra with codec/load/baseline/update/removed renderers; `SystemContext` opaque carrier, `Snapshot` durable comparison, `Generation` immutable baseline+snapshot; `combine`/`initialize`/`reconcile`/`replace`. `registry.ts` -- scoped `SystemContextRegistry.Service`. `builtins.ts` -- `core/environment` (cwd, workspace root, git status, platform) + `core/date`.

**Database** (`database/`, 47 files, 216KB): `database.ts` (orchestration: SQLite layer + PRAGMAs WAL/synchronous=NORMAL/busy_timeout/64MB cache/FKs + migration). `sqlite.bun.ts`/`sqlite.node.ts` (platform adapters: Bun `bun:sqlite` / Node 22+ `node:sqlite`, Semaphore-guarded, Drizzle ORM). `migration.ts`+`migration.gen.ts` (migration engine with Semaphore serialization, fresh vs incremental modes). `schema.gen.ts` (generated base schema: 16 tables with cascade-delete FKs). `path.ts` (cross-platform path column types). `migration/` (40 files, ~30KB -- timestamped migration scripts `{id, up}`, dynamically imported + applied sequentially; notable: event-sourced session input, context epoch, workspace, credential, normalize_storage_paths).

**Infrastructure services:**
- `event.ts` (25KB) -- Core event system (`@opencode/Event`): in-memory pub/sub + SQLite-backed durable event-sourced aggregates (transactional append, sequence checks, owner claims, idempotent replay, projector execution). Global node.
- `git.ts` (35.7KB) -- `Git.Service` (`@opencode/GitV2`): grouped interface (repo/remote/history/sync/change/worktree/index/tree), branded types, all git via `AppProcess` spawning, per-repository `KeyedMutex` serialization, tree diff/preview via temp index, worktree with dirty-state `forceRequired` detection.
- `process.ts` (9.9KB) -- `AppProcess`: extends `ChildProcessSpawner` + `run()` (buffered) and `runStream()` (line-delimited Stream), `maxOutputBytes` truncation.
- `cross-spawn-spawner.ts` (18.5KB) -- Node `cross-spawn` implementation, process group signaling w/ SIGKILL escalation, Windows `taskkill` fallback. Global node.
- `shell.ts` (6KB) -- Pure utilities: `killTree`, `gitbash()`, per-shell arg building (zsh/bash/fish/nu/cmd/PowerShell), shell discovery/selection.
- `fs-util.ts` (10.3KB) -- Effect `FileSystem` wrapper (`isDir`/`readJson`/`ensureDir`/`findUp`/...) + pure sync path helpers (`mimeType`/`normalizePath`/`contains`/`overlaps`).
- `npm.ts` (9.1KB) + `npm-config.ts` -- NPM package management via `@npmcli/arborist` reification, `EffectFlock` cross-process locking, bin resolution.
- `snapshot.ts` (10KB) -- Content-addressed filesystem snapshot service (Git-based storage in global data dir): `capture`/`files`/`diff`/`preview`/`restore`/`checkout`.
- `repository-cache.ts` (9.5KB) + `repository.ts` (6.4KB) -- Remote git repo cloning/caching into local cache (`EffectFlock` locking, status: cached/cloned/refreshed) + pure git ref parsing (`github:owner/repo` shorthand, SCP-style, URLs, `file:`).
- `file-mutation.ts` (7.5KB) -- Atomic file mutations (`create`/`write`/`writeTextPreservingBom`/`writeIfUnchanged`/`remove`) via `KeyedMutex`, BOM handling.
- `patch.ts` (7.6KB) -- Pure parser/applier for structured patch format with fuzzy matcher (exact/rstrip/trim/normalized).
- `background-job.ts` (12.4KB) -- Process-local in-memory job registry (start/extend/wait/promote/cancel), not durable.
- `models-dev.ts` (8.5KB) -- Fetches/caches provider/model catalog from models.dev (file-based cache, 5-min TTL, 60-min background refresh, fallback chain).
- `aisdk.ts` (8KB) -- Bridges OpenCode model config -> AI SDK `LanguageModelV3` instances, plugin hook registration, SSE-aware fetch w/ per-chunk timeout.
- `global.ts` (2.2KB) -- XDG-based directory layout, eagerly `mkdir`'d at import time.

**Location & composition:** `location.ts` (`Location.Service` w/ optional `vcs`, `boundNode(ref)` creates location-scoped layer), `location-services.ts` (composition root grouping all location-scoped services into `LayerNode.group`), `location-service-map.ts` (`LayerMap` keyed by `Location.Ref`), `location-mutation.ts` (path resolution for mutations, boundary containment, permission resource derivation), `instruction-context.ts` (System Context source discovering `AGENTS.md` upward from project dir).

**Domain services** (top-level Effect services):
- `session.ts` (20KB) -- `SessionV2.Service`: 17 methods (list/create/get/messages/prompt/.../resume/interrupt + nested revert). `prompt()` is `Effect.uninterruptible`: admits `session_input` row -> conditionally wakes execution unless `resume:false`. Race-safe creation. Deps: ProjectV2, WorkspaceV2, ModelV2, EventV2, Database, SessionProjector, SessionStore, SessionExecution, LocationServiceMap, SessionInput, SessionRevert, SessionRunner.
- `config.ts` (9.6KB) -- `Config.Service`: multi-location discovery (global -> directory ancestry for `opencode.jsonc`/`opencode.json`/`.opencode`), V1 detection+migration, jsonc-parser lenient decode. Location-scoped.
- `catalog.ts` (12.2KB) -- `Catalog`: central provider/model registry, policy-based filtering, cost/age heuristic for "small" model.
- `permission.ts` (11.8KB) -- `Permission`: wildcard rule engine (allow/deny/ask), `assert` blocks on `Deferred` until user replies, saved "always allow" rules, agent-scoped sets.
- `integration.ts` (21.5KB) -- `Integration`: registry of OAuth/key/env methods, OAuth authorization flows (auto+code modes), connection management + token refresh.
- `plugin.ts` (5.5KB) -- `PluginV2.Service`: `add`/`remove`/`wait` with keyed-mutex scoped lifecycle.
- `credential.ts` (4.8KB) -- `Credential` CRUD for OAuth tokens/API keys in SQLite.
- `reference.ts` (4.3KB) -- `Reference`: named filesystem/git-repository references, materializes via repository cache.
- `skill.ts` (4.7KB) -- `Skill`: stateful registry sourcing skills from dirs/URLs/embedded, frontmatter parsing, dedup by name.
- `question.ts` (4.9KB) -- `QuestionV2.Service`: `ask` (blocks uninterruptibly until answered), `reply`/`reject`, `list`.
- `pty.ts` (11KB) -- `Pty` session management via native bindings (create/attach/write/remove, 2MB buffer, 25-session cap).
- `ripgrep.ts` (10KB) -- `Ripgrep` adapter: find/glob/grep via `AppProcess`, JSON output parsing.
- `image.ts` (2.7KB) -- `Image` service: normalizes base64 images against size limits, lazy photon adapter.
- `account.ts` (3.5KB) -- Account/auth domain types + OAuth device-code flow types.
- `policy.ts` (1.6KB) -- Lightweight policy engine (wildcard matching on action+resource).
- `state.ts` (4.4KB) -- Generic scoped state management (transforms, `batch` coalescing, `Semaphore`).
- `schema.ts` (2.9KB) -- Utility Schema types + `DeepMutable` + `Newtype` branded wrapper factory.
- `agent.ts` (3.6KB) -- `AgentV2.Service`: in-memory agent registry (get/default/resolve/select/all + `State.Transformable`).
- `command.ts` (2KB) -- `CommandV2.Service`: slash-command registry (get/list + `State.Transformable`).
- `model.ts`/`provider.ts` -- Re-export barrels for schema types + mutable variants.

**Small leaf modules** (Drizzle tables + single-purpose services): `account/` (AccountTable/AccountStateTable), `credential/` (CredentialTable), `event/` (EventSequenceTable/EventTable), `permission/` (saved permissions + PermissionTable), `share/` (SessionShareTable), `id/` (time-sorted ID generation w/ configurable prefix), `image/` (Photon Rust WASM image adapter -- Lanczos3 resize), `oauth/` (branded HTML page generators for OAuth callback servers), `installation/` (build-time version constants), `observability/` (file logging + OTLP export + runID), `project/` (directories CRUD, git_worktree copy strategy, Drizzle project/project_directory tables, `Project.Service.resolve()` Git-identity), `control-plane/` (move-session service + workspace table), `flag/` (feature flag constants from env w/ `OPENCODE_EXPERIMENTAL` master toggle), `filesystem/` (7 files -- platform-aware file search: native `fff` binary for Bun + ripgrep/fuzzysort fallback, `@parcel/watcher` file watcher, ignore/protected path lists).

**Utilities** (`util/`, 18 files, 92KB): Larger -- `flock.ts`+`effect-flock.ts` (mkdir-based file locking w/ heartbeat anti-eviction, `EffectFlock.Service` w/ `LockTimeoutError`/`LockCompromisedError`), `encode.ts` (URL-safe base64, SHA-256, FNV-1a checksum), `error.ts` (schema-backed error factory `NamedError.create`), `which.ts` (PATH lookup), `wildcard.ts` (glob->regex), `retry.ts` (exp backoff), `slug.ts`, `path.ts`, `glob.ts`. Trivial helpers: `array`/`binary`/`hash`/`lazy`/`module`/`token`.

**Effect wiring** (`effect/`, 9 files, 48KB): `layer-node.ts` (12.3KB -- dependency-graph DSL: `Node` abstraction wrapping `Layer`+deps+`Tag`+`kind`, `compile()` wires tree, `hoist()` extracts subgraphs, type-level dependency validation). `app-node.ts` (two-tag system: `"location"` depends on `"global"`, `makeGlobalNode`/`makeLocationNode` factories). `app-node-builder.ts` (`build()` conditionally builds `LocationServiceMap`). `app-node-platform.ts` (5 pre-built global nodes: filesystem->httpClient->requestExecutor->llmClient). `keyed-mutex.ts` (per-key `Semaphore` w/ users counter). `runtime.ts` (lazy singleton `ManagedRuntime`). `service-use.ts` (Proxy-based `tag.use()`). `memo-map.ts`.

**V1 compatibility** (`v1/`, 19 files, 108KB): `config/config.ts` (monolithic V1 `Info` ~30 fields), `config/migrate.ts` (`isV1` heuristic + one-way migration to V2: mode->agents, permission+tools->permissions array, provider->providers, mcp restructure, compaction renames, skills flatten, plugin normalize), `config/provider-options.ts` (per-provider `Lowerer` interface: options->headers/body/url/settings for openai/anthropic/google/azure/bedrock/openaiCompatible), `permission.ts`/`session.ts` (V1 error types + schemas), `config/agent.ts` (V1 agent schema + `normalize()`), `config/lsp.ts` (~40 builtin server IDs), `config/mcp.ts`. Smaller passthrough: server/attachment/formatter/command; `layout.ts` (deprecated), `skills.ts`, `plugin.ts`.

**GitHub Copilot adapter** (`github-copilot/`, 25 files, 216KB) -- Adapts OpenAI Responses + Chat Completions APIs to the LLM protocol interface.
- `copilot-provider.ts` -- `createOpenaiCompatible()` factory: `.chat()`/`.responses()`/`.languageModel()`, merges auth headers + UA suffix.
- `chat/` (8 files, ~40KB) -- Chat Completions adapter: `openai-compatible-chat-language-model.ts` (28KB -- `LanguageModelV3` impl, `doGenerate`/`doStream` w/ SSE `TransformStream`, reasoning->text->tool-input ordering, Copilot-specific `reasoning_effort`/`verbosity`/`thinking_budget`), message converter, tool preparer, metadata extractor, finish-reason mapper.
- `responses/` (12 files, ~93KB) -- Responses API adapter (GPT-5+): `openai-responses-language-model.ts` (58.7KB -- `doGenerate`/`doStream`, 18+ SSE event types via type-guards, 8 hosted tool types, reasoning w/ encrypted content, 19 chunk schemas), input converter (system 3-mode, user text/image/PDF, assistant reasoning/tool-call/tool-result), tool preparer, API types.
- `tool/` (6 files, ~15KB) -- Provider-executed tool schemas: `code_interpreter` (Python sandbox), `file_search` (vector store), `image_generation`, `web_search_preview` (search/open_page/find), `web_search` (allowedDomains/userLocation), `local_shell` (server-side exec).

## packages/opencode/src/

V1 implementation of the OpenCode host: session orchestration, HTTP server, built-in tools, plugin system, and all domain services. All modules follow the Effect `*.Service` pattern; many share an `InstanceState` per-session state map and publish transitions through `EventV2Bridge`. V2 counterparts live in `packages/core/src/`.

- `session/` (39 files, 496KB) — REFINED — V1 session orchestration: the multi-step LLM conversation loop, tool execution, compaction, message persistence, and instruction resolution. Drives the CLI/TUI experience (V2 core lives in `packages/core/src/session/`). All modules follow the Effect `*.Service` pattern; several share an `InstanceState` `Map` for in-memory per-session state and publish state transitions through `EventV2Bridge`. The `llm/` subdir is the adapter seam between opencode-owned orchestration (`../llm.ts`) and the pluggable AI SDK / `@opencode-ai/llm` native runtime.
  - `prompt.ts` (64KB) — `SessionPrompt` service: orchestration core for the LLM-driven session loop. `prompt` (admit user message + enter loop), `loop` (multi-step assistant-tool loop via `SessionRunState`), `shell` (execute shell command under assistant message), `command` (resolve/execute command template as subtask), `resolvePromptParts` (expand file/agent references from markdown). `runLoop` fetches compacted history, resolves tools per agent, constructs system prompts (skills/env/MCP/instructions), handles subtask delegation, compaction overflow, emits events via `EventV2Bridge`.
  - `session.ts` (36KB) — `Session` service: V1 session CRUD and event-coordination. List/create/fork/mutate sessions; message+part management works purely through `EventV2Bridge` event publishing (no direct SQL). `getUsage` computes token-aware cost across provider metadata formats (Anthropic/Bedrock/Vertex/Venice/Copilot). `fork` with message-ID anchoring.
  - `processor.ts` (28KB) — `SessionProcessor.Service.create()` returns a `Handle` with `process()` that streams LLM response via `LLM.stream()`, handling events (reasoning/text/tool calls/step-start/finish/errors). Manages tool call lifecycle, reasoning parts, snapshot tracking, doom-loop detection, compaction overflow.
  - `message-v2.ts` (28KB) — `MessageV2`: message persistence + AI SDK model-message conversion. `toModelMessagesEffect()` converts `WithParts[]` → `ModelMessage[]`, handling role-based assembly, media-in-tool-result workarounds per provider, compaction parts, reasoning signature preservation. Pagination (`page`), streaming (`stream`), compaction filtering (`filterCompacted`), error classification (`fromError`).
  - `tools.ts` (24KB) — `SessionTools.resolve()`: builds AI SDK `Tool[]` from `ToolRegistry`, MCP tools, MCP resource tools (`list_mcp_resources`, `list_mcp_resource_templates`, `read_mcp_resource`). Each tool's `execute` wraps an Effect via `EffectBridge`, applies permission checks, triggers plugin hooks (`tool.execute.before`/`after`).
  - `compaction.ts` (20KB) — `SessionCompaction.Service`: context compaction/pruning. `isOverflow()`, `prune()` (removes old tool call output to free tokens), `process()` (compaction summarization via compaction agent + auto-continue), `create()` (inserts compaction marker). Smart tail-turn preservation (`preserveRecentBudget`/`turns`/`splitTurn`).
  - `llm.ts` (16KB) — `LLM.Service.stream()`: creates `Stream<LLMEvent>` via AI SDK (`streamText` with `wrapLanguageModel`) or experimental native runtime. `run()` prepares request (system prompts, tools, auth, provider options), wires GitLab Workflow Language Model tool execution, configures OpenTelemetry tracing.
  - `instruction.ts` (12KB) — `Instruction.Service`: discovers/reads `AGENTS.md`/`CLAUDE.md`/`CONTEXT.md` from global config + project trees + remote URLs. `system()` returns assembled instruction strings. `resolve()` walks upward from filepath to attach nearby instruction files once per message (deduplicated via `claims` map).
  - `system.ts` (6KB) — `SystemPrompt` namespace + `.Service`: `provider(model)` selects model-specific prompt text (`anthropic.txt`/`beast.txt`/`gpt.txt`/`gemini.txt`/...); `environment`/`skills`/`mcp` build `<env>`/skills/`<mcp_instructions>` blocks appended into the system prompt.
  - `summary.ts` (5KB) — `SessionSummary.Service`: `summarize`/`diff`/`computeDiff` extract snapshot diffs between step-start/step-finish parts of a user turn and store them on `message.info.summary.diffs`.
  - `run-state.ts` (5KB) — `SessionRunState.Service`: per-session `Runner` fiber registry (`InstanceState` Map). `assertNotBusy`/`ensureRunning`/`startShell`/`cancel` (cascades to background jobs).
  - `revert.ts` (6KB) — `SessionRevert.Service`: three-phase `revert`/`unrevert`/`cleanup` — walks messages to target, snapshots via `snap.track()`, restores previous snapshot, applies `snap.revert(patches)`, writes new diffs.
  - `retry.ts` (7KB) — `SessionRetry`: `retryable(error, provider)` (5xx always, `FreeUsageLimitError`/`GoUsageLimitGo` body parsing, rate-limit heuristics → `Retryable` w/ UI upsell action), `delay()` (exponential backoff w/ `retry-after` header priority), `policy` builds Effect `Schedule` integrating with processor retry state.
  - `message.ts` (5KB) — V1 message schema: `ToolCall`/`ToolPartialCall`/`ToolResult`/`ToolInvocation` (state-discriminated), `TextPart`/`ReasoningPart`/`ToolInvocationPart`/`SourceUrlPart`/`FilePart`/`StepStartPart`, top-level `MessagePart` union + `Info` (full message w/ metadata: timing, error, assistant model/provider/path/cost/tokens, snapshot). Re-exports `AuthError`/`OutputLengthError`.
  - `reminders.ts` (3KB) — `SessionReminders.apply`: injects system reminders into the last user message on agent mode transitions — `PROMPT_PLAN` (plan agent, experimental off), `BUILD_SWITCH` (plan→build), `PLAN_MODE` (experimental, references/creates plan file via `Session.plan()`).
  - `overflow.ts` (1KB) — `usable` (effective token budget = `model.limit.input` or `context - maxOutputTokens` minus 20K `COMPACTION_BUFFER`, overridable via `cfg.compaction.reserved`) + `isOverflow` (compares total tokens vs usable budget; no-op when `cfg.compaction.auto` off).
  - `todo.ts` (2.5KB) / `status.ts` (2KB) — Small `InstanceState`-backed services: `Todo.Service` (CRUD on `TodoTable`, publishes `todo.updated`) and `SessionStatus.Service` (in-memory status Map, publishes `session.status`/`session.idle`).
  - `llm/` (4 adapter files + `AGENTS.md`, ~32KB) — Adapter seam between `../llm.ts` (opencode-owned auth/config/plugin/permission) and the LLM runtime. `AGENTS.md` documents the runtime-selection flow and safety policy (AI SDK default; `OPENCODE_EXPERIMENTAL_NATIVE_LLM` flag gates native with fallback). `ai-sdk.ts` (9KB — `LLMAISDK.toLLMEvents` pattern-matches every AI SDK `fullStream` event into canonical `@opencode-ai/llm` `LLMEvent`s, mutable `adapterState()` for step/text/reasoning counters + Copilot billing extraction). `request.ts` (7.5KB — `LLMRequestPrep.prepare`: merges agent/plugin/provider options via `mergeDeep`, fires plugin hooks (`chat.system.transform`/`chat.params`/`chat.headers`), `strict:false` on OpenAI tools, `_noop` tool for Copilot replay, opencode project/session/request headers). `native-request.ts` (7.8KB — `LLMNative.request`/`model`: the only file permitted to build `@opencode-ai/llm` `LLMRequest`/`Message`/parts; maps AI SDK `npm` provider packages → `@opencode-ai/llm` `configure().model()`). `native-runtime.ts` (7.9KB — `LLMNativeRuntime.status`/`stream`: native gate (OpenAI/Anthropic/opencode-managed, rejects OAuth/missing-key), pipes `LLMClient.stream()` through parallel `ToolRuntime.dispatch` via `FiberSet`+`Queue`, injects OAuth fetch override).
  - (dropped — trivial: `schema.ts` branded `MessageID`/`PartID`, `message-error.ts` `OutputLengthError`/`AuthError` stubs)
- `server/` — REFINED — V1 HTTP server for the CLI/TUI host: Effect `HttpApiApp` layer stack w/ port fallback (4096→OS-assigned), mDNS, Basic auth, orderly disposal. `routes/instance/httpapi/` holds the HttpApi tree (`api.ts` composes Root/Event/Instance/Server/PtyConnect groups; `public.ts` generates OpenAPI spec; `errors.ts` declares `Schema.TaggedErrorClass` HTTP error contracts). `groups/` (21 files) — `HttpApiGroup` endpoint-contract declarations, the authoritative SDK-generated surface (`session.ts`: 24 endpoints, `experimental.ts`: 14, `tui.ts`/`instance.ts`: 12 each, `pty.ts`: 7+WS, plus `file`/`workspace`/`global`/`sync`/`provider`/`mcp`/`project`/`config`/`control`/`permission`/`question`/`control-plane`/`project-copy`/`event`). `handlers/` (20 files) — concrete handlers mirroring groups (`session.ts`: 27 endpoints, `pty.ts`: WS w/ ticket validation + buffered replay, plus `experimental`/`file`/`tui`/`global`/`provider`/`workspace`/`mcp`/`event`/`sync`/`instance`/`project`/...). `middleware/` (9 files) — `workspace-routing.ts` (RequestPlan: Local provides context, Remote proxies via `HttpApiProxy` + fence sync), `authorization.ts` (Basic auth + PTY ticket bypass), `proxy.ts`, `compression.ts`, `fence.ts`, `schema-error.ts`, `error.ts` (defect→500 w/ ref ID). `shared/` (6 files) — embedded web UI bundle w/ CSP, fence sync, workspace routing, PTY tickets, TUI control channel.
- `tool/` (43 files, 312KB) — REFINED — Built-in V1 tools available to the AI agent. Each tool is a self-contained `makeLocationNode` performing one action (editing, reading, searching, shell, web, etc.) and returning typed output via the `define()` pattern with Effect schema validation + `Truncate` integration. Large tools (>8KB) are listed individually; the many ~4KB leaf tools share the same shape (resolve path → check `external_directory` permission → perform work → return schema-validated output) and are summarized in groups below.
  - **Tool framework**: `tool.ts` (8KB — `Context`/`ExecuteResult`/`Def`/`Info` types, `define()` helper w/ `InvalidArgumentsError` + truncation wiring), `registry.ts` (20KB — `ToolRegistry.Service`: collects/initializes all built-in + custom/plugin tools from `{tool,tools}/*.{js,ts}`, `tools(model)` dynamically enables/disables per model e.g. `apply_patch` only for GPT non-4, triggers plugin `tool.definition` hooks), `json-schema.ts` (8KB — `ToolJsonSchema`: Effect `Schema.Top` → JSON Schema 7, caches, normalizes `anyOf`/`allOf`, strips nulls, inlines `$ref`s), `schema.ts` (`ToolID` branded schema), `truncate.ts` (8KB — `Truncate` service: truncates outputs over line/byte limits, spills overflow to files, 7-day cleanup), `truncation-dir.ts` (`TRUNCATION_DIR` → `<data-dir>/tool-output`).
  - **File editing**: `edit.ts` (24KB — `EditTool`: resilient string-replacement w/ fallback replacers exact/line-trimmed/block-anchor/whitespace-normalized/indentation-flexible, per-file semaphore, LSP diagnostics, file-change events), `write.ts` (4KB — `WriteTool`: BOM-aware create/overwrite, diff for permission review, Format service, filesystem events), `apply_patch.ts` (12KB — `ApplyPatchTool`: unified-diff parser hunk add/update/delete/move, existence + external-directory checks, LSP diagnostics), `read.ts` (16KB — `ReadTool`: streaming line-based file/dir reader, 50KB byte-cap, 2000-char line truncation, binary detection, image/PDF base64 data URLs, `Instruction` context per file, LSP warming, similar-filename suggestions).
  - **Shell** (`shell/`): `shell.ts` (20KB — `ShellTool`: `ChildProcessSpawner` w/ tree-sitter parsing of bash/PowerShell/cmd for permission scoping, sliding-window output buffer spilling to disk, timeout/abort, exit codes), `shell/prompt.ts` (20KB — `ShellPrompt`: model-facing description + param schema specialized per shell type, usage guidance, chain operators, path-separator examples, git/GitHub workflow instructions), `shell/id.ts` (`ShellID`: bash/pwsh/powershell/cmd → canonical `Kind`, tool ID stays `"bash"` for compat).
  - **Search**: `grep.ts` (`GrepTool`: ripgrep regex content search, ≤100 results, external-dir perms), `glob.ts` (`GlobTool`: ripgrep glob file listing, ≤100, instance-relative paths).
  - **Web**: `websearch.ts` (8KB — `WebSearchTool`: routes to Exa/Parallel via hash or flag split, MCP-style HTTP), `webfetch.ts` (8KB — `WebFetchTool`: configurable text/markdown/html, Cloudflare challenge retry, Turndown HTML→Markdown, image attachments), `mcp-websearch.ts` (shared JSON-RPC `tools/call` + SSE/JSON client for Exa & Parallel).
  - **Subagent & planning**: `task.ts` (16KB — `TaskTool`: delegates to subagent in child session, foreground blocks via `BackgroundJob.wait` / experimental `background:true`, derives child perms denying `todowrite`/self-reference/config-specified primary tools), `code-mode.ts` (12KB — `CodeModeTool`/`execute`: agentic script in confined interpreter w/ MCP-tool access, groups by server, permission checks + plugin hooks + progress), `plan.ts` (`PlanExitTool`/`plan_exit`: asks to switch plan→build agent, injects approved-plan message on yes).
  - **Supporting tools** (≤4KB each, thin wrappers): `lsp.ts` (8KB — `LspTool`: proxies 9 LSP ops w/ 1-based positions), `todo.ts` (`TodoWriteTool`: session todo-list JSON), `skill.ts` (`SkillTool`: loads skill by name, ≤10 related files via ripgrep, XML envelope), `question.ts` (`QuestionTool`: `Question` service key-value answers), `invalid.ts` (`InvalidTool`: degenerate always-succeed fallback for invalid LLM args), `external-directory.ts` (permission helper for files outside project dir → prompts parent-dir glob).
- `config/` — REFINED — V1 config discovery/parsing + TUI config layer. `config.ts` `@opencode/Config` service: global (`~/.config/opencode/*`), remote well-known from accounts, per-project via `ConfigPaths`, `.opencode` plugin dirs, `OPENCODE_CONFIG_CONTENT` env, console/org managed, macOS MDM plists; legacy TOML→JSON migration, `$schema` injection on first write. `tui.ts` `@opencode/TuiConfig` merges `tui.json[c]` from multiple sources; `tui-migrate.ts` one-time extraction of legacy theme/keybinds. Smaller: `parse.ts` (JSONC parser + Schema validation), `paths.ts` (discovery walking up to worktree), `agent.ts`/`command.ts`/`plugin.ts` (dir scanners), `variable.ts` (`{env:VAR}`/`{file:path}` substitution), `managed.ts` (macOS plist reader).
- `permission/` — REFINED — V1 wildcard permission rule engine. `index.ts` `Permission.Service` w/ `ask`/`reply`/`list` on per-instance pending queue; `evaluate()` resolves permission+pattern against config+approved rulesets. `arity.ts` derives human-meaningful command-prefix arity via generated `ARITY` dictionary for shell permission scoping.
- `plugin/` (19 files, 252KB) — REFINED — V1 plugin system: provider-specific auth plugins mapping provider auth flows (OAuth/API key) to the common LLM protocol, plus TUI plugin runtime, plugin loading/installation, OpenAI WebSocket pooling, and plugin metadata. All OAuth auth plugins spin up local HTTP callback servers and handle token refresh; they differ in flow type (device-code/PKCE/implicit), provider-specific request patching, and catalog/model integration.
  - `tui/runtime.ts` (32KB) — TUI plugin runtime: lifecycle manager for all TUI plugins. `RuntimeState` with plugin registry, loading internal/external plugins, activation/deactivation with scoped lifecycle (keymap/attention/mode/event handlers/slots/themes). Plugin disposals serialized with timeout guard; keymap registrations auto-scoped.
  - **OAuth auth plugins** — Provider-specific OAuth flows via local callback servers with JWT/token refresh. `xai.ts` (24KB — Grok: loopback + RFC 8628 device-code, PKCE S256, port 56121, single-flight refresh dedup); `snowflake-cortex.ts` (20KB — dynamic-port loopback, account-ID normalization, role scoping, `max_tokens`→`max_completion_tokens` + SSE role patching); `openai/codex.ts` (20KB — ChatGPT Pro/Plus loopback+device-code, experimental WebSocket transport via `OpenAIWebSocketPool`, GPT-5.4+ filter, 400K context, `ChatGPT-Account-Id` claim); `github-copilot/copilot.ts` (16KB — device-code, dynamic model fetch, GitHub Enterprise, `x-initiator`/`Copilot-Vision-Request` headers); `digitalocean.ts` (12KB — implicit grant, Inference Routers fetch w/ 5min cache, router+base model merge).
  - `shared.ts` (12KB) — Plugin resolution/validation: parse specifiers (npm/file/alias), entrypoint resolution via `package.json` exports, `engines.opencode` semver compat, V1 module reading, deprecated-name handling.
  - **OpenAI WebSocket transport** (`openai/ws.ts` 12KB + `openai/ws-pool.ts` 12KB) — `OpenAIWebSocket` low-level helpers (`connectResponsesWebSocket` proxy+timeout, `streamResponsesWebSocket` WS→SSE w/ retryable errors, idle timeout) + `OpenAIWebSocketPool` connection pool (`createWebSocketFetch` intercepts `POST /responses` w/ `stream=true`, per-session reuse, age/idle pruning, failure-count HTTP fallback).
  - `loader.ts` (12KB) — `PluginLoader`: resolution pipeline Plan→Resolved→Loaded, on-demand install, dynamic import, parallel external plugin loading.
  - `install.ts` (12KB) — Plugin install + config patching: spec→target resolution, manifest reading, JSONC-aware config editing w/ flock locking.
  - `index.ts` (12KB) — `Plugin.Service`: main entry. Loads built-in auth plugins (Codex/Copilot/GitLab/Poe/Cloudflare/Azure/DigitalOcean/Snowflake/xAI) then external plugins from config. Exposes `trigger`/`list`/`init` w/ dispose cleanup.
  - `github-copilot/models.ts` (12KB) — Copilot model fetching/normalization: `/models` API Schema → OpenCode `Model` format w/ capabilities/limits/pricing/reasoning variants, model merging.
  - `meta.ts` (8KB) — `PluginMeta`: persistent metadata store (touch/touchMany records load events — first/last time, fingerprint, version — to JSON w/ flock; `setTheme` mapping).
  - **Thin wrappers** (~4KB each) — `tui/internal.ts` wraps `createBuiltinPlugins` from `@opencode-ai/tui`; `pty-environment.ts` provides `PtyEnvironment.Service` via `shell.env` plugin hook; `cloudflare.ts` (Workers AI API key + AI Gateway w/ `max_tokens` strip for reasoning); `azure.ts` (Azure OpenAI API key w/ optional resource-name prompt).
- `auth/` — REFINED — `@opencode/Auth` service persisting provider credentials (OAuth/API key/well-known token) to `{dataDir}/auth.json` w/ `0o600` perms; respects `OPENCODE_AUTH_CONTENT` env injection.
- `account/` — REFINED — V1 account/login domain: `account.ts` `@opencode/Account` service (OAuth 2.0 device-code login, token polling, eager pre-expiry refresh, org fetch; refresh-dedup `Cache`), `repo.ts` DB layer on `AccountTable`/`AccountStateTable`, `schema.ts` branded types (`AccountID`/`OrgID`/`AccessToken`/`DeviceCode`), `url.ts` `normalizeServerUrl` canonicalizer.
- `provider/` (5 files, 156KB) — REFINED — V1 provider abstraction layer: a central `Provider.Service` registry merges catalogs from `ModelsDev`, config providers, and env/API keys, then resolves models to AI SDK `LanguageModelV3` instances; sibling modules own request/response payload transformation, authentication flows, and error classification. (`model-status.ts` is a thin `ModelStatus` union re-export — `alpha`/`beta`/`deprecated`/`active` — for lifecycle filtering.)
  - `provider.ts` (76KB) — `Provider.Service` (`@opencode/Provider`): central provider registry + model resolution. `list`/`getProvider`/`getModel`/`getLanguage` (resolves AI SDK `LanguageModelV3`)/`closest` (fuzzy search)/`getSmallModel` (cheapest by family priority)/`defaultModel`. Custom per-provider logic (Azure/Bedrock/Vertex/GitLab/Cloudflare). AI SDK providers lazily loaded via `BUNDLED_PROVIDERS` map + cached.
  - `transform.ts` (56KB) — Request/response payload transformation. `normalizeMessages()` scrubs surrogates, filters empty content (Anthropic/Bedrock), truncates tool-call IDs, injects/extracts `reasoning_content` (DeepSeek). `variants()` maps reasoning effort levels per provider SDK (OpenAI/Anthropic adaptive/Google thinking/SAP/Bedrock/Groq/Mistral). `sanitizeOpenAISchema()`/`sanitizeGemini()` lower JSON Schema 7 to provider-accepted forms. `applyCaching` for Anthropic/Bedrock/OpenRouter.
  - `error.ts` (8KB) — Error classification: `parseAPICallError` (AI SDK `APICallError` → `context_overflow`/`api_error` w/ retryability), `parseStreamError` (structured streaming payloads), `message()` extraction (JSON fields, HTML gateway/proxy errors). `HeaderTimeoutError`/`ResponseStreamError`.
  - `auth.ts` (8KB) — `ProviderAuth.Service`: OAuth + API-key authorization flows driven by plugin `auth` hooks. `methods()` (available auth methods per provider), `authorize()` (initiates OAuth → URL + instructions), `callback()` (completes OAuth or stores API key). Typed text/select prompts + conditional `when` guards.
- `agent/` — REFINED — V1 built-in agents + prompt templates + subagent permissions. `agent.ts` `Agent.Info` schema + `@opencode/Agent` registry of built-ins (`build`/`plan`/`general`/`explore`/`compaction`/`title`/`summary`) w/ hardcoded permission presets, merges `cfg.agent` overrides, `generate()` calls `generateObject` for new agent definitions. `prompt/*.txt` hidden-agent system prompts. `subagent-permissions.ts` derives subagent ruleset from parent deny + external_directory + own ruleset.
- `command/` — REFINED — V1 slash-command registry aggregating built-in templates + `cfg.command` + MCP prompts + skills; `hints()` parses `${N}`/`$ARGUMENTS` placeholders. `template/initialize.txt` (AGENTS.md creation/improvement), `template/review.txt` (severity-structured review scoped by `$ARGUMENTS`).
- `skill/` — REFINED — V1 skill registry discovering skills from global `.claude`/`.agents`, project dirs, `skills.paths`, remote URLs, `.opencode` dirs; frontmatter parsing, dedup by name. `discovery.ts` `SkillDiscovery` fetches `index.json` manifest w/ concurrent download + version-based cache updates (atomic staging/rename rollback).
- `git/` — REFINED — `Git.Service` wrapping git CLI: `branch`/`defaultBranch`/`status`/`diff`/`stats`/`patch`/`patchAll`/`patchUntracked`/`applyPatch` w/ opinionated flags; `Result` captures exit code/output/truncation.
- `mcp/` — REFINED — Model Context Protocol integration: `index.ts` `MCP.Service` manages local (stdio) + remote (streamable HTTP/SSE) server lifecycle w/ automatic OAuth flow, stores tools/prompts/resources; subscribes to `ToolListChanged`/`LoggingMessageNotification` for live updates. `oauth-provider.ts` implements MCP SDK `OAuthClientProvider` (persistent + pending-in-memory versions), `oauth-callback.ts` lightweight HTTP callback server on `127.0.0.1:19876` w/ CSRF state + 5-min timeout, `auth.ts` `McpAuth.Service` persistent OAuth store (`mcp-auth.json` w/ flock locking), `catalog.ts` tool discovery w/ cycle-detecting pagination + `convertTool()` → AI SDK `dynamicTool`, `browser.ts` system browser launcher.
- `image/` — REFINED — `@opencode/Image` service normalizing base64 data-URL images via Photon WASM w/ dimension/byte limits + iterative resize (PNG + multi-quality JPEG candidates).
- `snapshot/` — REFINED — `Snapshot.Service`: per-project hidden git repos under data dir tracking worktree file state independent of user VCS. `track()`/`patch()`/`diffFull()`/`restore()`/`revert()` w/ batched `git cat-file --batch`; seeds from source repo alternates, ignores gitignored + >2MB files, periodic `gc --prune=7.days`.
- `share/` — REFINED — V1 session sharing: `share-next.ts` `@opencode/ShareNext` live sharing w/ remote server (listens to session events via `EventV2Bridge`, debounced 1s flush queue, full+differential HTTP snapshots); `session.ts` auto-shares created sessions based on config/runtime flags.
- `storage/` — REFINED — V1 JSON KV store under app data dir: read/write/update/remove/list w/ per-file reentrant `TxReentrantLock`; runs data migrations on first access (restructuring old project/session/message layouts, extracting diff summaries).
- `sync/` — REFINED — V1 sync/event-sourcing design: `README.md` design doc for `SyncEvent` recording/replaying w/ total ordering + auto-republish as `Bus` events; `schema.ts` `EventID` branded schema.
- `worktree/` — REFINED — `Worktree.Service` creates/lists/removes/resets git worktrees as isolated dev envs: slugified names, `git worktree add -b opencode/<name>`, `InstanceStore.load` bootstrap, `WorktreeEvent.Ready`/`Failed`; `reset` does `reset --hard` + `git clean -ffdx` + submodule reset.
- `background/` / `bus/` — REFINED — `background/job.ts` thin adapter wrapping core `BackgroundJob` in `InstanceState` scope; `bus/global.ts` process-global `EventEmitter` `GlobalBus` w/ auto-injected ascending event IDs + typed scope fields (`directory`/`project`/`workspace`).
- `cli/` (87 files, 1004KB) — REFINED — V1 CLI: yargs-based command tree (`cmd/`), in-terminal interactive rendering engine for `opencode run` (`cmd/run/`), debug inspectors (`cmd/debug/`), and TUI worker bootstrap.
  - **Top-level** — `bootstrap.ts` (loads InstanceRuntime for a dir, runs callback, disposes), `effect-cmd.ts` (yargs command factory whose handler is an Effect w/ auto-provisioned `InstanceRef`; `CliError`+`fail()`), `error.ts` (`FormatError` pattern-matcher mapping tagged errors → user strings), `heap.ts` (RSS poll → V8 heap snapshot if >2GiB, `OPENCODE_AUTO_HEAP_SNAPSHOT`), `network.ts` (yargs options for port/hostname/mdns/cors + `hasArg`/`hasBooleanArg`), `ui.ts` (ANSI print/println/logo/error/input helpers + `UICancelledError`), `upgrade.ts` (auto-update check via `Installation`, emits `UpdateAvailable`/`Updated` on `GlobalBus`, silent patch upgrades), `effect/prompt.ts` (Effect wrappers around `@clack/prompts`), `tui/worker.ts` (TUI worker entrypoint: heap monitor + RPC interface for fetch/snapshot/server/checkUpgrade/reload/shutdown), `tui/layer.ts` (calls `@opencode-ai/tui` `run()` w/ `AppNodeBuilder.build`), `tui/validate-session.ts` (validates session ID format + existence via SDK). (dropped: `logo.ts` — trivial re-export)
  - `cmd/` — Top-level command handlers. **Largest** (≥20kb): `github.handler.ts` (GitHub Actions runner: `githubInstall` wizard writes `.github/workflows/opencode.yml`; `githubRun` routes issue_comment/PR/issues/schedule events through opencode sessions, GraphQL PR/issue fetch, git checkout/branch/commit/push, PR creation w/ share links), `run.ts` (`opencode run`: non-interactive single-prompt w/ event streaming, interactive local `--mini`/remote `--attach`; session create/resume/fork, file attachment, stdin pipe, JSON events, model/agent selection, sharing), `mcp.ts` (MCP server config: `add`/`list`/`auth`/`logout`/`debug` w/ OAuth, JSONC-preserving edits), `providers.ts` (provider credential `list`/`login`/`logout` w/ built-in providers, plugin auth hooks, URL well-known auth). **Mid-size** (8-16kb): `stats` (sessions/messages/cost/tokens w/ per-model+per-tool usage + ASCII bar charts), `uninstall` (data/cache/config dir removal + PATH cleanup), `tui` (default `$0` command: Web Worker for server, RPC, network/auth, session validation; `--mini` → `runMini`), `export` (session → JSON w/ `--sanitize` redaction), `agent` (`create` wizard: LLM-generated prompt, permissions, MD frontmatter), `account` (device-code OAuth `login`/`logout`/`switch`/`orgs`/`open`), `session` (`list` + `delete`), `pr` (`gh pr checkout` + fork remotes + session import), `plug` (plugin install: npm resolve + manifest + config patch), `import` (session from JSON/ShareNext URL), `attach` (attach to running server by URL), `web` (start server + open web UI). **Thin wrappers** (≤4kb each): upgrade, serve, generate, db, models, acp, cmd, github, github.shared, prompt-display.
  - `cmd/debug/` — Debug subcommands registered via `index.ts` barrel (also inlines `info` [version/OS/terminal/plugins] + `paths` [Global.Path entries]). Subcommands: `agent` (resolve agent → tools w/ perms, optional single-tool exec w/ JSON params, or full agent config dump), `config` (full project config JSON), `file` (`search`/`read`/`list` via FileSystem), `lsp` (`diagnostics`/`symbols`/`document-symbols`), `ripgrep` (`files`/`search` → JSON), `snapshot` (`track`/`patch`/`diff`), `skill` (list skills), `scrap` (list projects), `startup` (perf timing), `v2` (V2 Catalog: providers/all/default/small model).
  - `cmd/run/` (38 files, ~480KB) — In-terminal interactive rendering engine for `opencode run` on @opentui/solid (SolidJS). Split-footer architecture: retained scrollback surface + footer (composer/panel/statusline) + runtime orchestrator.
    - **Data layer**: `session-data.ts` (core reducer `reduceSessionData`: SDK event → `SessionData` mutation, text buffering, bash echo stripping, permission/question queues, tool lifecycle), `subagent-data.ts` (parallel subagent tabs w/ per-child `SessionData`+commit frames + memory limits), `session-replay.ts` (rebuilds history from stored messages via reducer → scrollback commits + turn-summary markers + transient-local-row weaving + active-text preservation across resize), `session.shared.ts` (user-turn text + file mentions → prompt history ring; recent variant resolution).
    - **Transport**: `stream.transport.ts` (SDK event subscription + background `watch` loop → session-data reducer; `runPromptTurn()` w/ Wait/tick/cross-check turn completion; subagent routing, resize replay, question recovery, layer/scope lifecycle), `stream.ts` (thin bridge: reducer output → `FooterApi` calls).
    - **Tool display**: `tool.ts` (per-tool rules: view/run/scroll/permission/snap hooks for 19 tool types [bash/write/edit/apply_patch/task/read/glob/grep/lsp/webfetch/websearch/...]).
    - **Scrollback**: `scrollback.surface.ts` (retained-surface streaming append engine `RunScrollbackStream`: `ActiveEntry` accumulates content, commits settled rows block-by-block, markdown/code/text ranges, theme hot-swap, spacer insertion, turn-summary rows), `scrollback.writer.tsx` (`RunEntryContent` component + writers for text/code/markdown/5 snapshot types [code/diff/task/todo/question] + coalescing helpers), `scrollback.shared.ts` (color/syntax helpers: `entryLook`/`entrySyntax`/`entryColor`), `entry.body.ts` (`StreamCommit` → renderable `RunEntryBody`: user `›` prefix, reasoning code blocks, tool delegation, markdown).
    - **Footer** (split-footer architecture): `footer.ts` (`RunFooter` implementing `FooterApi`: SolidJS signals for all footer state, microtask-coalesced commit queue → `RunScrollbackStream`, view transitions, two-press interrupt/exit, theme refresh), `footer.view.tsx` (`RunFooterView` component: composer/panel + menu + statusline; routes between prompt/subagent-select/queued-prompt/command-palette/skill/model/variant/permission/question bodies; statusline shows mode/spinner/interrupt/usage/model/context-hints), `footer.prompt.tsx` (prompt composer `createPromptState`: key bindings, history nav, shell-mode `!` prefix, `@` autocomplete files/agents/resources, `/` slash-command completion, external editor, extmark-based mention tracking), `footer.command.tsx` (panel bodies: command palette, model/variant/subagent/queued-prompt/skill selects w/ fuzzysort + keyboard nav), `footer.permission.tsx` (three-stage permission UI: permission→always→reject w/ diff + reject textarea), `footer.question.tsx` (single + multi-question tabbed w/ confirm review), `footer.menu.tsx` (reusable `RunFooterMenu` grouped/flat list w/ category headers + keyboard nav), `footer.subagent.tsx` (subagent inspector: scrollable commit list w/ status/title/nav), `footer.width.ts` (responsive breakpoints for dialog/statusline sections by terminal width).
    - **Runtime**: `runtime.ts` (`runInteractiveRuntime` orchestrator: boot resolves SDK/TUI config/variant, creates lifecycle, starts stream transport, loads agent/resource/command catalog, runs prompt queue until exit; exports `runInteractiveMode`/`runInteractiveLocalMode`), `runtime.lifecycle.ts` (split-footer lifecycle: `CliRenderer` w/ `screenMode:"split-footer"`, theme, entry splash, `RunFooter` construction, SIGINT two-press exit, `close()`/`onResize`/`resetForReplay`), `runtime.queue.ts` (serial prompt queue `runPromptQueue`: footer prompt events → one turn at a time via `input.run()`, `/new`/`/exit`/`/quit` handling, queued-prompt edit/remove, per-turn duration), `runtime.boot.ts` (boot resolution: `resolveModelInfo`/`resolveSessionInfo`/`resolveRunTuiConfig` concurrent), `runtime.stdin.ts` (TTY stdin resolution, `/dev/tty`/`CONIN$` fallback), `runtime.shared.ts` (`reusePendingTask` dedup helper).
    - **State machines** (`*.shared.ts`): `permission.shared.ts` (three-stage permission state machine + `permissionInfo` w/ tool-specific formatting + `external_directory`/`doom_loop`), `question.shared.ts` (single/multi-question flow transitions + final `QuestionReply`), `prompt.shared.ts` (prompt history ring nav + helpers), `variant.shared.ts` (model variant resolution: CLI > saved > session-history; `cycleVariant`; persisted to `model.json`).
    - **Theme/rendering**: `theme.ts` (terminal palette query → system theme w/ gray interpolation + Oklab quantization → `RunTheme` [footer/entry/splash/block/syntax/diff colors] + fallback), `splash.ts` (entry/exit splash banners w/ cell-based logo renderer), `demo.ts` (`--demo` mode: synthetic SDK events [markdown/reasoning/bash/write/edit/patch/task/todo/question/permission/error] through real pipeline + slash commands `/permission`/`/question`/`/fmt`), `turn-summary.ts` ("▣ agent · model · duration" scrollback commit), `trace.ts` (dev JSONL event trace `OPENCODE_DIRECT_TRACE`), `types.ts` (split-footer type vocabulary: `StreamCommit`/`FooterView`/`FooterState`/`FooterEvent`/`FooterApi`/`RunPrompt`/`RunInput`), `prompt.editor.ts` (`/editor` integration: `resolveEditorSlashValue` + `realignEditorPromptParts` mention re-positioning).
- `format/` — REFINED — V1 code formatters: `formatter.ts` declarative registry of 25+ language formatters w/ extension matching + async `enabled()` checking `$PATH`/config/npm deps; `index.ts` `Format.Service` initializes from config, `file()` runs matching formatters, per-project `InstanceState` caches resolved commands.
- `patch/` — REFINED — Custom multi-hunk patch parser/applier (`*** Begin/End Patch`, `*** Add/Delete/Update File:`, `@@` chunks). Multi-pass line matching (exact→rstrip→trim→Unicode-normalized) tolerating whitespace/smart-quote diffs. `maybeParseApplyPatch`/`...Verified` detect patch invocations from CLI args + bash heredocs. Standalone functions, no `*.Service`.
- `question/` — REFINED — V1 question/ask service: `Question.Service` pending queue of user-facing prompts keyed by `QuestionID`; `ask`/`reply`/`reject`/`list` w/ `Deferred`-based answers, rejects pending on layer disposal.
- `project/` — REFINED — V1 project + instance lifecycle: `project.ts` `@opencode/Project` service (`fromDirectory` resolves via V2 `ProjectV2.resolve` + SQLite upsert, `list`/`get`/`update`/`initGit`, sandbox mgmt), `vcs.ts` `@opencode/Vcs` per-instance branch tracking + diff/patch w/ `HEAD`-file watching → `VcsEvent.BranchUpdated`, `instance-store.ts` per-directory `InstanceContext` cache w/ `Deferred` dedup + `reload`/`dispose`/`disposeAll`, `bootstrap.ts` eagerly yields all per-instance services so `R=never`.
- `installation/` — REFINED — `Installation.Service` detects install method (`npm`/`yarn`/`pnpm`/`bun`/`brew`/`scoop`/`choco`/`curl`) via `execPath` + installed packages; `latest()` queries upstream API; `upgrade()` dispatches method-specific command w/ typed errors.
- `control-plane/` — REFINED — Workspace/worktree control-plane: `workspace.ts` `Workspace.Service` full lifecycle (create/delete/list/session-warp) + bidirectional remote sync via SSE w/ exponential-backoff reconnection; `sessionWarp` handles cross-workspace transfer w/ VCS patch copy + event replay + claiming. `adapters/` per-project adapter registry + built-in `WorktreeAdapter` (lazy-loaded). `workspace-adapter-runtime.ts` Effect bridge wrapping adapter methods.
- `effect/` — REFINED — V1 Effect runtime/wiring: `runner.ts` `Runner` state machine for shell-then-run sequences, `app-runtime.ts` top-level `ManagedRuntime` composing ~50 `LayerNode` deps via `AppNodeBuilderV1.build`, `instance-state.ts` per-directory state via `ScopedCache`, `bridge.ts` Effect↔Promise bridge capturing refs/ALS, `runtime-flags.ts` ~25 `OPENCODE_` feature flags, `instance-ref.ts` `InstanceRef`/`WorkspaceRef` `Context.Reference` tags.
- `env/` / `id/` — REFINED — `env/index.ts` `@opencode/Env` mutable per-instance environment snapshot via `InstanceState`; `id/id.ts` compact sortable KSUID-like IDs w/ domain prefix (6-hex-byte timestamp+counter + 20 base62 random).
- `util/` — REFINED — V1 shared utilities. Larger: `filesystem.ts` (sync/async FS helpers, Windows path normalization, `findUp`/`globUp`), `process.ts` (`spawn` w/ abort/timeout/kill, `run` buffers, Windows `taskkill`), `repository.ts` (git ref parsing: SCP-style/HTTPS/`github:`/`file:` → `RemoteReference`). Smaller helpers: `archive.ts`, `bom.ts`, `data-url.ts`, `media.ts` (MIME + magic-byte sniff), `proxy-env.ts`, `queue.ts` (`AsyncQueue` + fixed-concurrency `work`), `rpc.ts` (JSON-RPC for web workers), `wildcard.ts` (glob match), `local-context.ts` (`AsyncLocalStorage`), `lazy.ts`, `signal.ts`, `timeout.ts`, `defer.ts`, `effect-http-client.ts`, `iife.ts`, `html.ts`. Re-exports from `@opencode-ai/tui`/core: `error.ts`/`locale.ts`/`record.ts`/`token.ts`.
- `acp/` — REFINED — Agent Client Protocol server (JSON-RPC over stdio) letting editors (Zed) drive the agent. `service.ts` (37.8KB — central orchestrator: `make()` factory, session mgmt via `ACPSession`, directory-snapshot caching, MCP server registration, prompt execution w/ slash-command detection e.g. `/compact`, model/mode/variant config, `restoreFromMessages` reconstructing config from history, error→ACP-error conversion, `loadDirectorySnapshot`/`selectDefaultModel`). `event.ts` (9.7KB — `Subscription` polls `global.event()` stream, dispatches `permission.asked`→`ACPPermission`, streams text/reasoning/tool updates back via ACP `sessionUpdate`; `replayMessage` for historical replay). `tool.ts` (10.4KB — pure converters: builders per tool status (pending/running/completed/error/duplicate), `toToolKind` (execute/edit/search/read/think/other), `toLocations` (file paths/shell cwd), image-attachment extraction, shell output dedup). `usage.ts` (8.4KB — `buildUsage` from assistant token counts, `totalSessionCost`, `findContextLimit`, `Service` caches context limits + pushes `usage_update` w/ tokens/limit/cost). `session.ts` (7.8KB — in-memory `Ref<Map>` session store w/ model/variant/mode getters+setters, part-metadata recording for replay). `directory.ts` (7.1KB — cached `Snapshot` (providers/models/variants/modes/commands) `Loader`+`Service` w/ `SynchronizedRef` caching). `content.ts` (6.9KB — bidirectional ACP content block ↔ V1 `PromptPart`/message-part conversion, handles `file://`/`zed://`/`data:` URIs, audience/annotation flags). `config-option.ts` (5.9KB — builds `SessionConfigOption` selectors for model/effort/mode, `parseModelSelection` `providerID/modelID[/variant]`). `permission.ts` (7.5KB — forwards `permission.asked` to ACP `requestPermission` callback, per-session serial queue, computes/writes proposed file diffs for edit perms, once/always/reject replies). `agent.ts` (2.6KB — thin adapter bridging ACP SDK `Agent` methods → `service.ts`, `Effect`→`RequestError` via `ACPError.toRequestError`). `error.ts` (3.4KB — 9 tagged errors + `toRequestError` mapping to `@agentclientprotocol/sdk` `RequestError` variants (invalidParams/authRequired/methodNotFound/internalError)). `profile.ts` (1.3KB — conditional perf-profiling gated by `OPENCODE_ACP_PROFILE=1`, logs `[acp-profile]` timing to stderr).
- `index.ts` — REFINED — Main CLI entrypoint bootstrapping `yargs` w/ 20+ subcommands (`run`/`generate`/`serve`/`web`/`mcp`/`acp`/`tui`/`session`/`plugin`/`db`/...), global middleware (log level, pure mode, heap tracking), forced `process.exit()` in `finally`.
- `event-v2-bridge.ts` — REFINED — `Service` wrapping core `EventV2.Service`: enriches events w/ ambient `InstanceRef`+`WorkspaceRef` location metadata, re-emits on `GlobalBus`, splits durable events into `"sync"` payload for cross-process sync.
- `node.ts` / `event-manifest.ts` / `temporary.ts` — REFINED — `node.ts` minimal re-export barrel (Node-agnostic public API: `Config`/`Server`/`bootstrap`/`Database`); `event-manifest.ts` re-exports event `Definitions`/`Durable`/`Latest`; `temporary.ts` stripped-down TUI-only CLI entrypoint.

## packages/tui/src/ (REFINED)

TUI package built on SolidJS + `@opentui/solid` rendering a terminal UI for the OpenCode agent. Composes an app shell, keymap/editor/clipboard/audio integrations, reusable dialog/component library, theme system, route components, feature plugins, SolidJS context stores, and utilities. All UI is built on reactive Solid primitives; plugin slots (`home_*`/`sidebar_*`/`app_*`) allow extension. Most dialogs follow the same shape: a `DialogSelect`-based modal with static `.show()` factory returning a `Promise<T>`.

**Top-level entrypoints** — Main app shell and integrations. `app.tsx` — main app shell: `run` Effect bootstraps `@opentui/solid` w/ nested provider hierarchy + mounts `App` orchestrating plugin startup, command palette, `useBindings` keymap wiring, Home/Session routing, SDK event listening. `parsers-config.ts` — 30+ tree-sitter parser entries mapping filetypes → WASM URLs + highlight/locals SCM queries. `editor-zed.ts` — Zed editor integration via `db.sqlite` selection queries. `attention.ts` — desktop notifications + soundboard w/ 6-sound pack + terminal-focus suppression. `keymap.tsx` — keymap system on `@opentui/keymap`: mode stack, leader-key timing, comma bindings, textarea input layer. `clipboard.ts` — cross-platform clipboard w/ image support via OSC 52 + native. `editor.ts` — `openEditor` spawns `$VISUAL`/`$EDITOR` + WS editor connection discovery. `terminal-win32.ts` — Windows console mode mgmt via `bun:ffi` kernel32. Trivial: `audio.ts`, `logo.ts`, `runtime.tsx`, `index.tsx` (barrel).

**`component/`** — Reusable TUI components: stack-based `Dialog`/`DialogSelect` modal system w/ static `.show()` factories, animated backgrounds, command palette, spinners, and full-screen error/loading surfaces. Larger (>10KB): `bg-pulse-render.ts` — animated "GO" logo framebuffer compositor, `dialog-provider.tsx` — full provider connection flow w/ OAuth auto/code + API-key auth, `dialog-move-session.tsx` — project-directory `DialogSelect` for moving sessions, `dialog-session-list.tsx` — session list grouped by recency w/ debounced search. Mid-size (5-10KB): `dialog-workspace-create.tsx`, `error-component.tsx`, `dialog-model.tsx`, `dialog-status.tsx`, `dialog-retry-action.tsx`, `dialog-workspace-file-changes.tsx`. Smaller dialogs (≤4KB): `dialog-console-org`, `dialog-workspace-list`, `dialog-session-delete-failed`, `dialog-debug`, `bg-pulse`, `dialog-stash`, `dialog-mcp`, `command-palette`, `dialog-workspace-unavailable`, `dialog-skill`, `logo`, `startup-loading`, `dialog-theme-list`, `dialog-tag`, `spinner`, `dialog-variant`, `dialog-session-rename`, `todo-item`, `dialog-agent`, `workspace-label`, `plugin-route-missing`, `use-connected`, `register-spinner`.

**`theme/`** — Theme system: flat semantic-token `Theme` type (~45 RGBA tokens) + `ThemeJson` schema + `resolveTheme` w/ cycle detection + runtime registries (defaults < plugins < custom < system). `generateSystem` derives full `ThemeJson` from terminal ANSI colors; `getSyntaxRules` maps tokens to 60+ TextMate-scope rules. 33 predefined color theme JSON assets (catppuccin, dracula, gruvbox, nord, tokyonight, solarized, monokai, synthwave84, ...).

**`routes/`** — Route components: home/landing + session chat view w/ supporting panels. `session/index.tsx` — primary session route: `Session()` orchestrates conversation view, fetches session data on mount, wires keymaps, renders message transcript in a `scrollbox` w/ `stickyScroll="bottom"`. Streaming via `streaming={true}`; `ReasoningPart` w/ collapsible `ReasoningHeader`. Tool calls via `ToolPart` + `PART_MAPPING` + `Dynamic` to 14+ tool components. `session/permission.tsx` — three-stage permission workflow; `session/question.tsx` — multi-question forms w/ tab navigation. Supporting: `home.tsx`, `home/session-destination.tsx`, `session/subagent-footer.tsx`, `session/dialog-message.tsx`, `session/footer.tsx`, `session/sidebar.tsx`, `session/dialog-fork-from-timeline.tsx`, `session/dialog-timeline.tsx`, `session/dialog-subagent.tsx`.

**`feature-plugins/`** — Built-in TUI feature plugins registered via `builtins.ts` → `createBuiltinPlugins()` returning `BuiltinTuiPlugin[]` (sidebar panels + home widgets + system: Notifications/PluginManager/WhichKey/DiffViewer). Larger: `system/diff-viewer.tsx` — VCS diff viewer route w/ split/unified view, `system/which-key.tsx` — categorized paginated keybinding panel, `home/tips-view.tsx` — ~120-entry rotating tips array. Mid-size: `system/diff-viewer-file-tree-utils.ts`, `system/plugins.tsx`, `system/diff-viewer-file-tree.tsx`. Smaller: `system/notifications.ts`, `system/diff-viewer-ui.tsx`, `sidebar/footer.tsx`, `sidebar/mcp.tsx`, `sidebar/files.tsx`, `sidebar/context.tsx`, `sidebar/lsp.tsx`, `sidebar/todo.tsx`, `home/footer.tsx`, `home/tips.tsx`, `builtins.ts`.

**`ui/`** — Dialog/toast/spinner/link UI primitives on `@opentui/solid`. `dialog-select.tsx` — generic `DialogSelect<T>` selectable-list dialog w/ `fuzzysort` filtering, `spinner.ts` — Knight-Rider/scanner animated spinner engine, `dialog-export-options.tsx`, `dialog.tsx` — core modal. Smaller: `dialog-confirm.tsx`, `dialog-prompt.tsx`, `dialog-help.tsx`, `dialog-alert.tsx`, `toast.tsx`, `border.ts`, `link.tsx`.

**`config/`** — TUI config schema + keybinding registry. `keybind.ts` — central keybinding registry: `Definitions` maps every named keybind → default keys + description; `index.tsx` — Effect `Schema` for TUI config fields, `TuiConfigProvider`/`useTuiConfig`.

**`context/`** — SolidJS context providers/stores for app-wide state, all using `createSimpleContext` pattern. Larger: `sync.tsx` — v1/v1.5 reactive store mirroring server state, `data.tsx` — v2 counterpart keyed by `LocationRef`, `local.tsx` — persisted local UI state, `editor.ts` — WebSocket connection to external editor. Mid-size: `theme.tsx`, `sdk.tsx`. Smaller: `project.tsx`, `runtime.tsx`, `route.tsx`, `kv.tsx`, `thinking.ts`, `event.ts`, `helper.tsx`, `path-format.tsx`, `clipboard.tsx`, `permission.tsx`, and others.

**`util/`** — Small pure/shared TUI helpers. Larger: `error.ts` — `cliErrorMessage` unwraps tagged errors, `transcript.ts`, `filetype.ts`, `locale.ts`, `selection.ts`. Smaller: `persistence.ts`, `signal.ts`, `presentation.ts`, `system.ts`, `layout.ts`, `format.ts`, `scroll.ts`, `revert-diff.ts`, `model.ts`, `tool-display.ts`, `provider-origin.ts`, `record.ts`, `path.ts`, `renderer.ts`, `collapse-tool-output.ts`, `session.ts`.

**`plugin/`** — TUI-side plugin runtime shim bridging to core plugin host. `adapters.tsx` — `createTuiApiAdapters(Input)` maps internal services → `TuiPluginApi`. Smaller: `command-shim.ts`, `runtime.tsx`, `slots.tsx`, `api.ts`.

**`prompt/`** — Prompt-input composition: history, stash, frecency, display/parts/traits. `history.tsx`, `stash.tsx`, `frecency.tsx`. Smaller: `display.ts`, `part.ts`, `traits.ts`.

## packages/app/src/ (REFINED)

SolidJS web app shell: route tree, 20+ context providers, reusable dialog/components, i18n (18 locales), and WSL integration. Uses `@tanstack/solid-query`, Sentry, and Tailwind CSS.

- `app.tsx` — Main app shell: route tree (`/` home, `/session/:id`, `/new-session`, `/directory/*`), legacy vs new layout, ~20 context providers.
- `entry.tsx` — Bootstrap: SolidJS render, locale detection, Sentry init.
- `desktop-menu.ts` — Desktop menu bar type system with platform-specific accelerators.

**`components/` (~1000KB)** — 80+ reusable UI components: 20+ dialogs (command palette, provider selection, project editing, fork, model management, settings), prompt input with attachments/drag-and-drop, titlebar with tab navigation, file tree with virtual scrolling, settings panels, session components, terminal, debug bar.

**`pages/` (~800KB)** — Route-level pages: home/project selection, main session page with file tabs/composer/terminal, legacy and new layout shells, session sub-pages (composer, v2 review, timeline with virtual scrolling).

**`context/` (~640KB)** — SolidJS state management (all use `createSimpleContext`): global/server/tabs/layout/file/prompt/settings/platform contexts, global sync engine (19 files, ~240KB) with event reducer and session cache, plus command/comments/permission/notification/MCP/sync contexts.

**`i18n/` (~380KB)** — 18 locale dictionaries (`en.ts` base + 17 translations) + `parity.test.ts`.

**`utils/` (~148KB)** — 46 utility modules: agent, AI message parsing, base64, diffs, file management, persistence, prompts, server health/scope, session routing, sound, terminal, time, toast, worktree.

**`wsl/` (~71KB)** — Windows Subsystem for Linux integration: types, context provider, server probes, dialogs, settings.

**`addons/` (25KB)** — xterm.js terminal serialization addon for ghostty-web.

**`hooks/`** — `use-providers.ts`, `provider-catalog.ts`.

**`constants/`** — `file-picker.ts`: accepted image/file types for OS dialogs.

**`assets/`** — Static assets: `help/introducing-tabs.mp4`, `help/placeholder.png`.

## packages/ui/src/ (REFINED)

Shared SolidJS UI component library (1641 files): 40+ v1 components + 30+ v2 components, algorithmic theming engine (v1 + v2), i18n (17 locales), markdown rendering with Shiki. Components use `data-component` attribute convention with co-located CSS.

**`components/` (~520KB)** — V1 component library using Kobalte headless primitives: button, dialog, dropdown-menu, context-menu, tabs, select, file-icon, provider-icon, app-icon, icon, diff-changes, toast, tooltip, popover, switch, checkbox, radio-group, avatar, card, progress, spinner, tag, badge, accordion, collapsible, scroll-view, list, keybind, inline-input, text-field, favicon, logo, motion-spring, animated-number, image-preview, dock-surface, sticky-accordion-header.

**`v2/` (~336KB)** — Next-gen component system with redesigned visuals: 30+ `-v2` suffixed components (accordion, avatar, badge, button, checkbox, dialog, diff-changes, divider, field, file-tree, icon-button, inline-input, keybind, line-comment, loader, menu, progress-circle, project-avatar, radio, segmented-control, select, switch, tabs, text-input, textarea, toast, tooltip, wordmark, tab-state-indicator) + v2 design tokens CSS.

**`context/` (~28KB)** — SolidJS context providers: `createSimpleContext<T>()` factory, `DialogProvider`, `I18nProvider` (translation with `{{param}}` interpolation), `MarkedProvider` (markdown with Shiki + KaTeX), `FileComponentProvider`, `WorkerPoolProvider`.

**`hooks/` (~8KB)** — `create-auto-scroll.tsx` (smart auto-scroll with user-pause detection), `use-filtered-list.tsx` (fuzzy-search list with `fuzzysort`).

**`i18n/` (~72KB)** — 17 locale dictionaries: `en.ts` (195+ keys, authoritative) + 16 translations.

**`theme/` (~148KB)** — Algorithmic theming engine: color math (hex↔Oklch, scale generation), token resolution (~200 CSS tokens from seed/palette), `ThemeProvider` (lazy-loads, caches, system/light/dark), 37 bundled themes, v2 theme resolution (WCAG contrast, semantic tokens).

**`styles/` (~40KB)** — Global CSS: master stylesheet with layers, CSS reset, design tokens, gray scales, animations, utilities, Tailwind CSS v4 integration.

**`storybook/` (~8KB)** — Scaffold wrapper + sample fixtures.

**`assets/` (~2MB)** — Static binaries: fonts (Inter, JetBrains Mono), favicons (multiple sizes), icons (app, file-types, provider), images, 90 notification sounds.

## packages/web/src/ (REFINED)

Starlight-based documentation and landing site for OpenCode: marketing landing page, multilingual documentation (17 locales), and real-time session share viewer. Built with Astro + SolidJS.

- `middleware.ts` — Astro middleware for locale routing: detects `/docs/en/` style URLs, rewrites to proper locale paths, parses `Accept-Language` with `matchLocale()`.

**`assets/` (~800KB)** — Static media: brand logos (dark/light/ornate SVG), lander icons/screenshots, web screenshots.

**`components/` (~400KB)** — Astro + SolidJS UI components: `Lander.astro` (marketing landing page with install commands, feature list, screenshot gallery), `Share.tsx` (real-time session replay over WebSocket with auto-reconnect), `Hero.astro`, `Header.astro`, `Footer.astro`, `Head.astro`, `SiteTitle.astro`, `LanguageSelect.astro`, 100+ Heroicons-style SVG icons, `share/` subcomponents (part renderer, content views for bash/code/diff/error/markdown/text, copy button).

**`content/` (500+ files, ~5MB)** — Astro content collections: `docs/` (30+ MDX documentation pages in 18 locales), `i18n/` (18 locale JSON files for UI strings).

**`content.config.ts`** — Astro content config: `docs` and `i18n` collections with type-safe schemas.

**`i18n/`** — `locales.ts`: locale arrays, alias mapping, `matchLocale()` parser.

**`pages/`** — Astro routes: `s/[id].astro` (session share page with Open Graph meta), `[...slug].md.ts` (API route returning raw Markdown).

**`styles/`** — `custom.css`: custom color palette, Starlight overrides, responsive breakpoints.

**`types/`** — Ambient type declarations for `lang-map` and Starlight virtual modules.

## packages/cli/src/ (REFINED)

- `commands/` — `commands.ts` declares the full CLI command tree (`api`, `debug agents`, `migrate`, `service start`/`restart`/`status`/`stop`/`password`, `serve`). `handlers/` (12 files) implements each: `api.ts` (3.4KB — HTTP requests via direct path or OpenAPI op ID resolution w/ param interpolation), `serve.ts` (2.1KB — starts v2 API server, auto-incrementing port from 4096, registers with daemon), `default.ts` (launches TUI via daemon transport), `migrate.ts` (stub), `api.test.ts` (resolveOperation/rawRequest tests), `service/` (5 files — start/stop/restart/status/password daemon ops), `debug/agents.ts` (prints registered agents JSON).
- `framework/` — `runtime.ts` (3.1KB — CLI execution framework on `effect/unstable/cli/Command`: `handler`/`handlers`/`run` wiring spec tree w/ lazy-loaded modules, injecting `Daemon.Service`); `spec.ts` (1.3KB — `Spec` wrapper around `Command.make` for declarative CLI tree building).
- `services/daemon.ts` — Core background process manager: persistent server process (detached child), registers URL/PID in state file, graceful stop (SIGTERM→SIGKILL), health + version compatibility checks, transport credentials, password persistence, server auto-registration w/ lifecycle cleanup.
- `index.ts` — Main entry: command handler registry mapping symbolic names → lazy-loaded modules, runs CLI with `Daemon` + `NodeServices` layers.
- `tui.ts` — `runTui`: launches TUI by connecting to running server via transport URL/headers; `gracefulFetch` returns legacy fallbacks for 404s (`/config/providers`, `/agent`).

## packages/client/src/ (REFINED)

- `index.ts` — Tiny barrel re-exporting `./generated/index` + `EventsSubscribeOutput` as `OpenCodeEvent`.
- `contract.ts` — Client-side HTTP API contract via Effect's HttpApi: declares `LocationMiddleware`/`SessionLocationMiddleware` service classes, calls `makeDefaultApi`, maps server group/endpoint names → REST pluralized paths; omitted endpoints (`fs.read`, `pty.connect`, `pty.connectToken`) handled raw.
- `effect.ts` — Re-export barrel: Effect-aware client imports from `./generated-effect/index` + schema types (Agent/Command/Credential/FileSystem/Session/etc.) from `@opencode-ai/schema/*` + `OpenCodeEvent`.
- `generated/` / `generated-effect/` (auto-generated) (dropped — boilerplate, do not edit directly)

## packages/server/src/ (REFINED)

- `api.ts` — Defines the server-side HTTP API by calling `makeDefaultApi` with the server's own `LocationMiddleware` and `SessionLocationMiddleware`, producing the `Api` used by all handler groups.
- `handlers.ts` — Composes all individual handler layers into a single merged `Layer` via `Layer.mergeAll`.
- `routes.ts` — Assembles the full server app: layers application services (Database, EventV2, SessionV2, PTY tickets, credentials), auth, location, authorization, schema-error middleware, and all handlers; exports `webHandler` (Effect router → fetch-compatible handler).
- `auth.ts` — Basic auth: `Config` service (configurable via `OPENCODE_SERVER_USERNAME`/`OPENCODE_SERVER_PASSWORD`), credential decoding, `authorized` check, helpers to produce `Authorization` headers for clients.
- `cors.ts` — CORS origin validation: allows localhost, Tauri, `*.opencode.ai` origins, and configurable custom origins via `CorsConfig`; `isAllowedRequestOrigin` with same-host matching.
- `location.ts` — Server `LocationMiddleware`: resolves workspace location from query params or `x-opencode-directory`/`x-opencode-workspace` headers, provides `LocationServiceMap` services; `response` helper wraps handler data with location info.
- `pty-environment.ts` — `PtyEnvironment.Service` abstraction for providing env vars to PTY processes, default no-op implementation.
- `handlers/` (18 files) — Concrete HTTP handler groups mirroring the protocol groups. Major handlers: `session.ts` (13.2KB — full Session REST API: list/create/get, switch agent/model, prompt, compact, wait, revert stage/clear/commit, context, history, SSE events, interrupt, messages, with error mapping to protocol types); `pty.ts` (8.7KB — PTY CRUD + `connectToken` + `connect` raw WebSocket: replays buffered output, streams live data, accepts input via custom binary protocol); `permission.ts` (3.7KB — permission request/saved endpoints: listing, session-scoped asks, reply/fetch, saved records); `integration.ts` (3.4KB — integration CRUD + OAuth/key connection flows + attempt lifecycle); `message.ts` (3.2KB — cursor-based pagination, base64url JSON cursors, ascending/descending); `project-copy.ts` (2.6KB — copy create/remove/refresh, domain errors → `ProjectCopyError`); `question.ts` (2.4KB — global/per-session question listing, reply/reject with ownership validation); `event.ts` (1.9KB — SSE subscribing to `EventV2.allBounded` with `server.connected` initial event + 15s heartbeat); `fs.ts` (1.3KB — `fs.read`/`fs.list`/`fs.find` scoped to location); `provider.ts` (1.1KB — list/fetch providers via `Catalog.Service`); `credential.ts` (0.8KB — label update / removal via Integration connection API); `location.ts` (0.6KB — current location info).
  - Single-endpoint `*.list` handlers — `model.ts`, `agent.ts`, `command.ts`, `reference.ts`, `skill.ts` each list available resources via their respective `*.Service`; `health.ts` returns `{ healthy: true }`.
- `middleware/` (3 files) — `authorization.ts` (2.6KB — HTTP Basic auth, `Authorization` header or `auth_token` query param, `UnauthorizedError` w/ `WWW-Authenticate`; PTY WebSocket with connect ticket bypasses credential checks); `session-location.ts` (2.5KB — resolves session's original location from DB via `sessionID` route param, provides `LocationServiceMap`); `schema-error.ts` (0.9KB — schema validation → `InvalidRequestError`, truncates reasons to 1024 chars).

## packages/protocol/src/ (REFINED)

- `api.ts` — Composes the entire `HttpApi` surface by importing all endpoint groups, applying location-scoped and session-scoped middleware, and annotating with OpenAPI metadata. Exports `makeApi` (accepts custom event definitions) and `makeDefaultApi` (built-in `EventGroup`), both parameterized by location and session-location middleware context keys.
- `errors.ts` — All HTTP error types via `Schema.TaggedErrorClass` with status codes: `InvalidRequestError` (400), `UnauthorizedError` (401), `ForbiddenError` (403), `ConflictError` (409), `ServiceUnavailableError` (503), `UnknownError` (500), plus domain errors (`ProviderNotFoundError`, `SessionNotFoundError`, `MessageNotFoundError`, `InvalidCursorError`, `PermissionNotFoundError`, `QuestionNotFoundError`, `PtyNotFoundError`).
- `groups/` — `HttpApiGroup` definitions per domain, all location-scoped via `LocationQuery`. **Major groups:** `session.ts` — 17 endpoints for full session lifecycle (list/create/get, switch agent/model, prompt, compact, wait, revert, context, history, SSE events, interrupt, messages); `permission.ts` — pending/saved permission listing + session-scoped operations, factory `makePermissionGroup`; `pty.ts` — PTY CRUD + connect tokens; `integration.ts` — integration list/fetch, connection flows, OAuth attempt lifecycle; `question.ts` — pending requests + session-scoped operations, factory `makeQuestionGroup`; `fs.ts` — `fs.read`/`fs.list`/`fs.find`; `event.ts` — SSE subscription `/api/event`, exports `EventGroup`/`OpenCodeEvent`; `message.ts` — paginated `session.messages`; `project-copy.ts` — experimental copy ops; `provider.ts` — list/fetch providers; `location.ts` — defines `LocationQuery` schema; `credential.ts` — update label / remove credential. **Single-endpoint groups:** `command.ts`, `model.ts`, `reference.ts`, `skill.ts`, `agent.ts` each define a single `X.list` endpoint; `health.ts` defines `health.get` (`/api/health` → `{ healthy: true }`).
- `middleware/` — `authorization.ts` (`Authorization` middleware → `UnauthorizedError`), `schema-error.ts` (`SchemaErrorMiddleware` → `InvalidRequestError` (400)). Both applied globally.

## packages/schema/src/ (REFINED)
## packages/schema/src/ (REFINED)

Core session/message schemas:
- `session.ts` — `Session.Info` struct (V2 metadata: id, projectID, agent, model, cost/token tracking, timestamps, location, revert state) + `Session.ListAnchor` pagination; re-exports `SessionID`/`SessionEvent`.
- `prompt.ts` / `prompt-input.ts` — Prompt schemas: `Prompt` struct (text + optional file/agent attachments) with `Source`/`FileAttachment`/`AgentAttachment`; `prompt-input` is the lighter inbound variant (no MIME).
- `session-input.ts` — `SessionInput.Admitted` record created at durable admit time (sequence, message ID, prompt, delivery mode, timestamps, optional promotion sequence).
- `session-message.ts` — Central V2 message schema: tagged union (`User`/`Synthetic`/`System`/`Shell`/`Assistant` with text/reasoning/tool-call blocks, `AgentSwitched`/`ModelSwitched`/`Compaction`); `ToolState` enum; branded `ID` (`msg_` prefix).
- `session-event.ts` — All V2 session event types via `Event.define` — durable (prompted, admitted, step/text/reasoning/tool lifecycle, compaction, retry, revert) + live-only deltas. Exports `Durable` (replay-safe) and `All` sets.
- `session-todo.ts` / `session-status-event.ts` / `session-compaction-event.ts` / `session-delivery.ts` — Minor session schemas: todo, status, compaction notification, delivery union.

Event infrastructure:
- `event.ts` — Foundational `Event.define` (schema structs w/ metadata wrappers), `Event.inventory`, `Event.latest`, `Event.durable` filter; branded `Event.ID` (`evt_` prefix).
- `event-manifest.ts` — Central registry aggregating all event definitions, organized into `ServerDefinitions` (foundation+features) layers; computes `Latest` map for version-aware dispatch.
- `durable-event-manifest.ts` — Extracts durable replay-safe definitions: `SessionDurable` (V2) and `Durable` (V1+V2 union).

Provider/model/agent/permission config schemas:
- `model.ts` — Model config: `Model.Ref`/`Capabilities`/`Cost`/`Api`/full `Model.Info`.
- `provider.ts` — Provider config: branded `Provider.ID`, `Provider.Api`/`Request`/`Info`.
- `agent.ts` — Agent schema: branded `Agent.ID`, `Agent.Color` union, `Agent.Info` (model ref, request config, system prompt, mode, visibility, color, step limit, permission ruleset).
- `permission.ts` / `permission-saved.ts` — V2 permission contract: `Permission.Request`/`Reply`/`Rule`/`Ruleset` + events; `permission-saved` is the persisted-grants record.
- `command.ts` — `Command.Info`: user-saved command template.
- `credential.ts` — Credential value model: `Credential.OAuth`/`Credential.Key` tagged union, branded `ID` (`cred_` prefix).
- `llm.ts` — Reusable LLM schemas: `ProviderMetadata`, `ToolTextContent`/`ToolFileContent`/`ToolContent` union.

Project/workspace/integration/other domain schemas:
- `project.ts` / `project-copy.ts` — `Project.Info` + `project.updated`; project-copy input/output schemas.
- `integration.ts` — Integration schema: auth methods (OAuth/Key/Env), interactive prompts, OAuth attempt lifecycle, `Integration.Ref`/`Info`, events.
- `reference.ts` / `skill.ts` / `question.ts` — Reference sources, skill sources, V2 question contract.
- `pty.ts` / `pty-ticket.ts` — PTY schema + `PtyTicket.ConnectToken` for WebSocket auth.
- `revert.ts` / `file-diff.ts` — Revert snapshot model + `FileDiff.Info`.
- `filesystem.ts` — `file.edited` event, `FileSystem.Entry`/`Submatch`/`Match`/`FindInput`.
- `location.ts` — `Location.Ref` (directory + optional workspaceID) + `Location.Info`, `Location.response` helper.
- `connection.ts` — Integration connection info: `Connection.CredentialInfo`/`EnvInfo` tagged union.

Standalone branded ID types (kept separate to prevent cycles):
- `session-id.ts` / `project-id.ts` / `workspace-id.ts` / `integration-id.ts` — Branded string types with time-sorted factory methods, 38-char identifiers.
- `identifier.ts` — Shared time-sorted ID generation utility.
- `schema.ts` — Foundational utilities: base types, custom `optional` combinator, `statics` helper, `DateTimeUtcFromMillis`.
- `index.ts` — Package barrel re-exporting all modules as named namespaces + raw `Prompt` types and `schema` utilities.

Minor event stubs: `workspace.ts`, `workspace-event.ts`, `filesystem-watcher.ts`, `project-directories.ts`, `tui-event.ts`, `mcp-event.ts`, `ide-event.ts`, `lsp-event.ts`, `server-event.ts`, `installation-event.ts`, `vcs-event.ts`, `worktree-event.ts`, `catalog.ts`, `plugin.ts`, `models-dev.ts`. Each is a small single-event or thin-reexport file.

V1 compat: `v1/` — Legacy V1 schemas for backward compat: `session.ts` (complete V1 message/event model), `question.ts`, `permission.ts`, `legacy-event.ts`.

## packages/llm/src/ (REFINED)

Pluggable LLM runtime: typed request/response schemas, per-provider configuration facades, per-protocol streaming implementations, and request routing with auth/transport. Every protocol implements the common `Protocol` interface translating provider-specific formats into the common `LLMEvent` stream; shared infrastructure supplies a common text/reasoning state machine and streaming tool-call accumulator.

**Top-level modules:**
- `llm.ts` — `LLM` namespace: `request()`/`updateRequest()`/`generateObject()` helpers building `LLMRequest` from user-friendly inputs.
- `tool.ts` / `tool-runtime.ts` — Core typed tool abstraction: `Tool.make()` (Effect schemas or raw JSON Schema), `ToolRuntime.dispatch()` executes one canonical call.
- `cache-policy.ts` — `applyCachePolicy()` injects `CacheHint`s onto request parts based on `CachePolicy`.
- `provider.ts` + `provider-error.ts` — `Provider.make()` structural helper for defining providers; context-overflow detection.

**`providers/`** — Per-provider configuration facades mapping provider-specific APIs to the common protocol interface. Each exposes a `configure().model(id)` factory (protocol + auth + endpoint). Built-in: `anthropic`, `amazon-bedrock`, `google`, `azure`, `openai` (Responses/WS/Chat), `openrouter`, `xai`, `github-copilot`, `cloudflare`, `openai-compatible.ts` (generic + family helpers), `openai-compatible-profile.ts`, `openai-options.ts`.

**`protocols/`** — Per-protocol streaming implementations (SSE/binary framing, message projection, tool-call streaming, usage/cache mapping):
- `anthropic-messages.ts` — Anthropic Messages: system as cache-control text blocks, full extended thinking, server-executed tools.
- `openai-responses.ts` — OpenAI Responses w/ HTTP SSE and WebSocket transports, 18+ SSE event types, 8 hosted tool types.
- `bedrock-converse.ts` — AWS Bedrock Converse (binary AWS event-stream framing), SigV4 auth.
- `openai-chat.ts` — OpenAI Chat Completions, reused by DeepSeek/TogetherAI/Cerebras/etc.
- `gemini.ts` — Google Gemini (`:streamGenerateContent?alt=sse`), tool schemas via `GeminiToolSchema.convert`.
- `shared.ts` — Central toolkit reused by every protocol: JSON codecs, token accounting, media validation, SSE framing.
- `bedrock-event-stream.ts` — Binary AWS event-stream decoding.
- `openai-compatible-chat.ts` — Minimal route reusing `OpenAIChat.protocol`.
- `utils/` — `lifecycle.ts` (common text/reasoning state machine), `tool-stream.ts` (streaming tool-call accumulator), `cache.ts`+`bedrock-cache.ts`, `bedrock-auth.ts`, `bedrock-media.ts`, `gemini-tool-schema.ts`, `openai-options.ts`, `tool-schema.ts`.

**`route/`** — Request routing: `client.ts` (central `Route` type + `LLMClient` service), `executor.ts` (sends requests, HTTP status→typed `LLMError`), `auth.ts` (`Credential` lazy resolution + composable `Auth`), `protocol.ts` (`Protocol` interface), `endpoint.ts`/`auth-options.ts`/`framing.ts`/`index.ts`. `transport/` — `http.ts` (`HttpTransport.httpJson`), `websocket.ts` (`WebSocketExecutor`), `index.ts`.

**`schema/`** — Common LLM Effect schemas: `events.ts` (`Usage` token accounting, 15 tagged `LLMEvent` types, `LLMResponse`), `messages.ts` (content parts, `Message` class, `ToolDefinition`/`ToolChoice`, central `LLMRequest`), `options.ts` (`ProviderOptions`/`HttpOptions`/`GenerationOptions`/`Model` class + `CacheHint`/`CachePolicy`), `errors.ts` (tagged error-reason classes + composite `LLMError`), `ids.ts` (branded `ModelID`/`ProviderID`, unions).

## packages/sdk/ (REFINED)

JS SDK package: hand-written client/server wrappers (`createOpencode()` convenience factory, server spawning, V2 client patching) with build tooling (regenerates V2 SDK from OpenAPI via `@hey-api/openapi-ts`). Auto-generated code and OpenAPI spec (1036KB) are dropped from detail.

## packages/console/ (REFINED)

Cloud backend console for OpenCode: authentication, billing, workspace management, API key management, usage tracking, and enterprise features. Built with SolidJS + SolidStart (app), shared core logic, email templates, and Cloudflare Workers functions.

- `app/` (~31MB, ~400 files) — SolidJS SolidStart web application. Route groups: `auth/` (OAuth via GitHub/Google), `workspace/` (admin console: dashboard, model toggles, BYO provider keys, subscription management, usage charts, API key CRUD, team management, billing), `black/` (premium subscription plan + Stripe checkout), `zen/` (OpenAI-compatible API proxy with rate limiting), `bench/` (LLM benchmark views), `api/` (enterprise contact, support). Also: shared UI components, SolidJS context providers, utilities, i18n, CSS, static assets.

- `core/` (~4.6MB) — Shared core logic. `schema/` (11 Drizzle ORM MySQL table definitions: account, auth, benchmark, billing, ip, key, model, provider, referral, user, workspace). Top-level modules: `account.ts`, `actor.ts` (request-scoped identity), `aws.ts` (SES email), `billing.ts` (full Stripe integration), `black.ts`/`lite.ts` (plan mapping), `key.ts` (API key management), `model.ts`, `provider.ts` (BYOK), `referral.ts`, `subscription.ts` (usage-limit analysis), `user.ts`, `workspace.ts`. `drizzle/` (transaction manager), `util/` (crypto, date, logging, memoization).

- `mail/` (324KB) — Email templates and sending: React-based HTML email template (`InviteEmail.tsx`) via `@jsx-email`, SES sending via aws4fetch.

- `function/` — Cloudflare Workers edge layer: `auth.ts` (OAuth via GitHub/Google), `log-processor.ts` (LLM inference telemetry to Honeycomb), `stat.ts` (stats API for rate limit data).

- `resource/` — SST resource adapter: `resource.cloudflare.ts` (Workers env), `resource.node.ts` (local dev via SST).

- `support/` (88KB) — Internal SolidStart SSR admin panel: workspace/subscription lookup, Stripe payments, usage stats.

## packages/desktop/ (REFINED)

Electron desktop app wrapping `packages/app`: a main process (window lifecycle, auto-updater, WSL/sidecar management), a preload bridge, and a SolidJS renderer. The sidecar runs the OpenCode server as a `utilityProcess` child (bundled via a virtual module, HTTP listener w/ CORS for `oc://renderer`). Configs: `package.json` (`@opencode-ai/desktop`, deps: electron-updater/store/log, marked, @zip.js, @lydell/node-pty + dev `@opencode-ai/app`), `electron-builder.config.ts` (3 release channels [dev/beta/prod] w/ distinct appIDs; macOS notarize+DMG, Windows NSIS+signing, Linux AppImage/deb/rpm), `electron.vite.config.ts` (main+preload[CJS]+renderer entries, node-pty platform resolution, WASM asset copy, optional Sentry). `AGENTS.md` notes renderer uses only `window.api`.

- `src/main/` — Electron main process. Core: `index.ts` (app entry: logging/crash/single-instance init, spawns sidecar on random loopback port w/ generated password, deep links, WSL servers, store migration, auto-updater, window restoration, full IPC wiring, macOS menu), `windows.ts` (`BrowserWindow` lifecycle: per-window state persistence, custom frame/titlebar per platform, zoom clamping, `oc://` secure file serving, unresponsive/recovery dialogs, pinch-zoom, titlebar overlay), `ipc.ts` (all IPC handlers: sidecar, store CRUD, file/dir pickers, clipboard image, notifications, zoom, titlebar, updater, desktop menu), `server.ts`+`sidecar.ts` (sidecar lifecycle: forks bundled server, `postMessage` start/stop/ready, health polling, env inheritance [shell env/proxy/loopback bypass]). Supporting: `logging.ts` (per-run log files, crash reporter, debug-log ZIP export), `shell-env.ts` (login-shell env probing, 5s timeout, skips nushell), `apps.ts` (external app [VS Code/terminal] path resolution per platform), `unresponsive.ts` (samples renderer JS call stacks on unresponsive window). Updater group: `updater.ts`+`updater-controller.ts`+`updater-subscriptions.ts` (`autoUpdater` wrapper w/ state machine [idle→checking→downloading→ready→installing], crash-recovery persistence, stops sidecar before install). Store group: `store.ts`+`store-cleanup.ts` (electron-store instances, stale-draft cleanup >30 days). Trivial: `menu.ts`/`markdown.ts`/`onboarding.ts`/`migrate.ts`/`window-registry.ts`/`desktop-menu-actions.ts`/`attachment-picker.ts`/`initialization.ts`/`constants.ts`/`store-keys.ts`/`env.d.ts`. Tests (`*.test.ts`) cover index, window-registry, updater-controller, store-cleanup, shell-env, attachment-picker, servers.
- `src/main/wsl/` — Windows Subsystem for Linux integration. `servers.ts` (central controller: distro lists/running sidecars/version checks, full lifecycle [install WSL/distros/opencode, add/remove/start/stop sidecars], electron-store persistence, state events), `runtime.ts` (low-level `wsl.exe`/`powershell.exe`/`cmd.exe` execution w/ timeouts + encoding detection + interactive pty; probes availability/distros/capabilities), `sidecar.ts` (spawns opencode server in WSL: free port + auth creds + PATH-sanitizing script, 30s health polling), `startup.ts` (auto-start, version validation, restart-pending, abortable health polling), `policy.ts` (pure utils), `ipc.ts` (Electron IPC bridge renderer↔controller; non-Windows stubs).
- `src/preload/` — `index.ts` (`contextBridge.exposeInMainWorld` → `window.api`: proxies all IPC + event listeners for menu/deep-links/updater/zoom), `types.ts` (`ElectronAPI` surface types).
- `src/renderer/` — Solid.js renderer entry. `index.tsx` (Sentry/i18n/updater/deep-link setup, `PlatformProvider` wiring Electron APIs into app layer, `DesktopMemoryRouter` persisting last URL per window, loads `AppInterface` from `@opencode-ai/app` w/ local sidecar + WSL servers as connection targets), `onboarding.tsx` (first-launch: waits for readiness, prompts project-dir pick), `webview-zoom.ts` (zoom signal: shortcuts, ctrl+wheel pinch, bidirectional main-process sync), `initialization.ts` (sidecar init Solid resource), `wsl/connections.ts` (maps ready WSL servers → sidecar connection objects; picks best default). `i18n/` (16 locales — boilerplate merged w/ `@opencode-ai/app` dictionaries).
# Run/build

- **Monorepo manager:** Bun workspaces + Turborepo (`turbo.json`). Use `bun install` at repo root.
- **Type checking:** Run `bun typecheck` from package directories (e.g., `packages/opencode`), never `tsc` directly.
- **SDK regeneration:** After changing public Protocol or Server `HttpApi`, run `bun run generate` from `packages/client`. Legacy JS SDK: `./packages/sdk/js/script/build.ts`. Do not edit `src/generated` or `src/generated-effect` directly.
- **Tests:** Cannot run from repo root; run from package dirs like `packages/opencode`.
- **Default branch:** `dev` (not `main`). Use `dev` or `origin/dev` for diffs.
- **Standalone executable:** `./packages/opencode/script/build.ts --single` from repo root → `./packages/opencode/dist/opencode-<platform>/bin/opencode` (e.g. `darwin-arm64`, `linux-x64`).
- **Run against a different directory:** `bun dev <directory>` targets a different repo/directory than the default `packages/opencode`; `bun dev .` runs OpenCode against itself.
- **Debugging:** Most reliable is `bun run --inspect=<url> dev ...` from a terminal. Breakpoints may not work with `bun dev` (runs server in a worker thread); use `bun dev spawn` instead, or debug server and TUI separately with `--inspect` and explicit ports.
- **Web app dev:** Start the server first (`bun dev serve`), then `bun run --cwd packages/app dev` separately (Vite on port 5173 needs the API server running).
- **Pre-push hook:** `script/hooks` installs a `pre-push` git hook running `bun install` + `bun run typecheck`.
